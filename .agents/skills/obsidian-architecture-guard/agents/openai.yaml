interface:
  display_name: "Obsidian Architecture Guard"
  short_description: "Guard architecture invariants and encoding discipline"
  default_prompt: "Use this skill when feature, model, pipeline, artifact, stage, or encoding work could create architecture drift."
