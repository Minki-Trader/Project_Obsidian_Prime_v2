interface:
  display_name: "Obsidian Architecture Guard"
  short_description: "Guard architecture, agent settings, repo-scoped skills, policy, artifact, path, and encoding discipline"
  default_prompt: "Use this skill when feature, model, pipeline, artifact, stage, agent settings, repo-scoped skills, policy edits, durable path references, or Korean encoding work could create architecture drift."
