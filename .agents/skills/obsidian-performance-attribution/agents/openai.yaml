interface:
  display_name: "Obsidian Performance Attribution"
  short_description: "Explain KPI changes through baseline, likely drivers, segment checks, trade shape, alternative explanations, confidence, and next probe."
  default_prompt: "Use $obsidian-performance-attribution when KPI or behavior changes; record observed change, comparison baseline, likely drivers, segment checks, trade shape, alternative explanations, attribution confidence, and next probe."
policy:
  allow_implicit_invocation: true
