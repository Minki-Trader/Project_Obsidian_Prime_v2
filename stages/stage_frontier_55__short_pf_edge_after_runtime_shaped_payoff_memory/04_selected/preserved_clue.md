# Frontier55 Preserved Clue(전선55 보존 단서)

- judgment(판정): `negative_memory_sparse_admission_runtime_veto_did_not_transfer(부정 기억, 희소 진입 허용 런타임 차단이 MT5로 전이되지 않음)`
- clue(단서): runtime-shaped score(런타임형 점수), sparse admission mask(희소 진입 허용 마스크), runtime veto tape(런타임 차단 테이프), MT5 order path(MT5 주문 경로)의 차이를 같은 장부에서 비교할 수 있게 했다.
- boundary(경계): clue only(단서 전용), no authority(권위 없음).
