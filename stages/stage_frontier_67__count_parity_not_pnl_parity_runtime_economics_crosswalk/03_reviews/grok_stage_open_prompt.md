# Grok Stage Open Prompt(F67 그록 단계 개방 프롬프트)

## Snapshot-Only Rules(스냅샷 전용 규칙)

Answer only from this prompt. Do not inspect files, run tools, browse, or perform local verification. Codex(코덱스)가 local verification(로컬 검증), execution(실행), and final claim(최종 주장)을 소유한다.

## Current Truth(현재 진실)

- F66 closed as `preserved_clue_negative_memory_no_authority(보존 단서 + 부정 기억, 권위 없음)`.
- F66 preserved clue(보존 단서): feature_ready_diff(피처 준비 차이)=0 and signal_count_diff(신호 수 차이)=0 for 64/64 MT5 split runs(분할 실행).
- F66 negative memory(부정 기억): PF>=2 only 1/64 split rows(분할 행), DD>10 in 60/64, max stage DD>10 in 31/32 executable stages(실행 단계).
- F66 did not rank L3/L4/L5 cause(3/4/5계층 원인 순위 미확정): order intent(주문 의도), fill/cost(체결/비용), KPI basis(KPI 기준) remain open.
- Five-stage retrospective(5단계 중간 검토) is not due after F66: counter(카운터) 1/5, next numeric trigger(다음 숫자 트리거) F70.

## Proposed F67 Direction(제안 F67 방향)

Open `stage_frontier_67__count_parity_not_pnl_parity_runtime_economics_crosswalk`.

F67 question(질문): why did exact signal count parity(정확한 신호 개수 동등성) fail to become PnL parity(손익 동등성), low DD(낮은 손실폭), or target trade density(목표 거래 빈도)?

Sequence(순서):

1. F67A DD basis crosswalk(손실폭 기준 대조).
2. F67B config parity depth pilot(설정 동등성 깊이 파일럿).
3. F67C runtime-native order intent economics(런타임 기반 주문 의도 경제성).

## Success Criteria(성공 기준)

- explain one or more proxy/runtime economics gap(프록시/런타임 경제성 간극) with bounded evidence(제한 근거).
- run at least one MT5 Runtime Probe(MT5 런타임 탐침) before F67 closeout(마감), unless logic impossibility(로직상 불가능) is recorded with repair action(수리 행동).
- keep completion/baseline/promotion/runtime authority/live readiness/Goal Achieve(완성/기준선/승격/런타임 권위/실거래 준비/목표 달성) forbidden(금지).

## Focus Question(집중 질문)

Critique the F67 opening direction. Is the question narrow enough, and should F67A start with DD basis crosswalk(손실폭 기준 대조) before config parity depth(설정 동등성 깊이)? Give concrete accepted/rejected/needs_local_verification(수용/거절/로컬 검증 필요) advice.
