# run338K Trade Count Recovery Expansion MT5 Probe(거래수 회복 확장 MT5 탐침)

## Summary(요약)

- run_id(실행 ID): `run338K_execute_trade_count_recovery_expansion_mt5_probe_without_db_v1`
- status(상태): `completed_stage338K_trade_count_recovery_expansion_mt5_probe_executed_review_required_no_selection`
- judgment(판정): `mt5_threshold_corridor_probe_outputs_available_review_required_no_selection`
- gates(게이트): `9/9`
- attempts(시도): `4`
- runtime_completed_rows(런타임 완료 행): `4`
- report_completed_rows(보고서 완료 행): `4`
- matched_rows(일치 행): `23308/23308`
- mismatch_rows(불일치 행): `0`
- best_attempt(최고 시도): `j02_p55_m00`
- best_net_profit(최고 순수익): `70.32`
- best_profit_factor(최고 수익 팩터): `1.84`
- best_recovery_factor(최고 회복 계수): `0.91`
- best_trade_count(최고 거래수): `21`
- external_verification_status(외부 검증 상태): `completed(완료)`
- next_run(다음 실행): `run338L_review_trade_count_recovery_expansion_mt5_probe_without_db_v1`

## Action(행동)

run338J(338J 실행)의 threshold corridor package(임계값 구간 패키지)를 MT5 Strategy Tester(MT5 전략 테스터)로 실행하고, telemetry(런타임 기록)를 expected tape(예상 테이프)와 비교했다.

Effect(효과): threshold(임계값) 조정이 trade count/recovery(거래수/회복 계수)를 실제 MT5(메타트레이더5)에서 개선하는지 검토할 근거를 만든다.

## Boundary(경계)

run338K(338K 실행)는 runtime probe attempt(런타임 탐침 시도)다. selected model(선정 모델), runtime authority(런타임 권위), operating promotion(운영 승격), Goal Achieve(목표 달성)는 없다.
