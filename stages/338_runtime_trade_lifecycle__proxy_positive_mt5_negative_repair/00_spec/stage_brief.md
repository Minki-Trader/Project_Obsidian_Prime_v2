# Stage338 Runtime Trade Lifecycle Repair(338단계 런타임 거래 생명주기 수리)

## Canonical Stage ID(정식 단계 ID)

`338_runtime_trade_lifecycle__proxy_positive_mt5_negative_repair`

## Stage Question(단계 질문)

Can timestamp-safe trade lifecycle labels/features/rules turn proxy-reproduced signals into positive MT5 KPI?
(시점 안전 거래 생명주기 라벨/피처/규칙이 재현된 신호를 양수 MT5 핵심 성과 지표로 바꿀 수 있는가?)

## Source Handoff(원천 인계)

- source_stage(원천 단계): `337_onnx_research_packet__cost_buffer_direction_curve_rebuild`
- source_run(원천 실행): `run337JR_review_runtime_positive_low_pf_recovery_drawdown_dual_probe_repair_mt5_runtime_probe_or_repair_without_db_v1`
- branch_run(분기 실행): `run338A_branch_stage337_to_runtime_trade_lifecycle_repair_without_db_v1`
- best_less_bad_model(가장 덜 나쁜 모델): `jn_jl_jk006_cost_stress_buffer_extratrees`
- best_mt5_net_profit(가장 덜 나쁜 MT5 순수익): `-191.49`
- best_mt5_profit_factor(가장 덜 나쁜 MT5 수익 팩터): `0.92`

## Scope(범위)

Stage338(338단계)은 Stage337(337단계)의 proxy-positive MT5-negative(프록시 양수 MT5 음수) 결과를 baseline(기준선)으로 쓰지 않는다.
Effect(효과): negative memory(부정 기억)를 제약으로 삼고, trade lifecycle repair(거래 생명주기 수리)를 새 주제로 가볍게 판다.

## Required Axes(필수 축)

- density throttle(밀도 제한)
- side loss quarantine(방향 손실 격리)
- cost-stress objective(비용 압박 목적)
- drawdown corridor(낙폭 통로)
- session/regime split(세션/국면 분할)
- Tier A/B paired records(Tier A/B 쌍 기록)

## Forbidden Claims(금지 주장)

No selected model(선정 모델 없음), no baseline(기준선 없음), no runtime authority(런타임 권위 없음), no operating promotion(운영 승격 없음), no Goal Achieve(목표 달성 없음).

## run338B Trade Lifecycle Repair Design(거래 생명주기 수리 설계)

- run_id(실행 ID): `run338B_design_runtime_trade_lifecycle_proxy_positive_mt5_negative_repair_without_db_v1`
- judgment(판정): `trade_lifecycle_repair_design_opened_from_valid_negative_runtime_probe_no_selection`
- design_variants(설계 변형): `5`
- next(다음): `run338C_materialize_runtime_trade_lifecycle_proxy_positive_mt5_negative_repair_inputs_without_db_v1`
- effect(효과): valid negative(유효한 부정)를 입력 생성 가능한 실험 설계로 바꿨다.

## run338B Trade Lifecycle Repair Design(거래 생명주기 수리 설계)

- run_id(실행 ID): `run338B_design_runtime_trade_lifecycle_proxy_positive_mt5_negative_repair_without_db_v1`
- judgment(판정): `trade_lifecycle_repair_design_opened_from_valid_negative_runtime_probe_no_selection`
- design_variants(설계 변형): `5`
- next(다음): `run338C_materialize_runtime_trade_lifecycle_proxy_positive_mt5_negative_repair_inputs_without_db_v1`
- effect(효과): valid negative(유효한 부정)를 입력 생성 가능한 실험 설계로 바꿨다.

## run338C Input Materialization(입력 생성)

- run_id(실행 ID): `run338C_materialize_runtime_trade_lifecycle_proxy_positive_mt5_negative_repair_inputs_without_db_v1`
- rows(행): `87666`
- features(피처): `56`
- next(다음): `run338D_review_runtime_trade_lifecycle_proxy_positive_mt5_negative_repair_inputs_without_db_v1`
- effect(효과): 거래 생명주기 수리 입력을 학습 전 검토 가능하게 만들었다.

## run338D Input Review(입력 검토)

- run_id(실행 ID): `run338D_review_runtime_trade_lifecycle_proxy_positive_mt5_negative_repair_inputs_without_db_v1`
- original_overlap_timestamp_count(기존 겹친 타임스탬프 수): `1`
- repaired_overlap_timestamp_count(수리 뒤 겹친 타임스탬프 수): `0`
- next(다음): `run338E_train_runtime_trade_lifecycle_repair_models_group_safe_without_db_v1`
- effect(효과): run338E(338E 실행)가 group-safe split(묶음 안전 분할)로 학습하게 한다.

## run338D Input Review(입력 검토)

- run_id(실행 ID): `run338D_review_runtime_trade_lifecycle_proxy_positive_mt5_negative_repair_inputs_without_db_v1`
- original_overlap_timestamp_count(기존 겹친 타임스탬프 수): `1`
- repaired_overlap_timestamp_count(수리 뒤 겹친 타임스탬프 수): `0`
- train_feature_count(학습 피처 수): `53`
- next(다음): `run338E_train_runtime_trade_lifecycle_repair_models_group_safe_without_db_v1`
- effect(효과): run338E(338E 실행)가 group-safe split(묶음 안전 분할)로 학습하게 한다.

## run338E Group-Safe Training Proxy(묶음 안전 학습 프록시)

- run_id(실행 ID): `run338E_train_runtime_trade_lifecycle_repair_models_group_safe_without_db_v1`
- models(모델): `3`
- best_proxy_model(최고 프록시 모델): `logreg_balanced_c025`
- next(다음): `run338F_review_group_safe_onnx_proxy_scores_for_mt5_probe_without_db_v1`
- effect(효과): ONNX(온엑스) 산출물을 만들되 MT5(메타트레이더5) 검증 전 선택으로 올리지 않는다.

## run338F Proxy Review Runtime Collapse(프록시 검토 런타임 축약)

- run_id(실행 ID): `run338F_review_group_safe_onnx_proxy_scores_for_mt5_probe_without_db_v1`
- collapsed_proxy_net_log_return(축약 프록시 순로그수익): `0.0713393579`
- next(다음): `run338G_materialize_runtime_collapsed_onnx_mt5_probe_package_without_db_v1`
- effect(효과): MT5(메타트레이더5) 전 timestamp duplicate(타임스탬프 중복)를 통제했다.

## run338G MT5 Probe Package(MT5 탐침 패키지)

- run_id(실행 ID): `run338G_materialize_runtime_collapsed_onnx_mt5_probe_package_without_db_v1`
- attempt(시도): `g01_logreg_balanced_c025_p60_m00`
- rows(행): `5827`
- next(다음): `run338H_execute_runtime_collapsed_onnx_mt5_probe_without_db_v1`
- effect(효과): MT5 runtime probe(MT5 런타임 탐침) 실행 준비 파일을 만들었다.

## run338H MT5 Runtime Probe(MT5 런타임 탐침)

- run_id(실행 ID): `run338H_execute_runtime_collapsed_onnx_mt5_probe_without_db_v1`
- external_verification_status(외부 검증 상태): `completed(완료)`
- matched_rows(일치 행): `5827`
- mismatch_rows(불일치 행): `0`
- effect(효과): 실제 MT5 실행 또는 차단 사유를 다음 검토로 넘긴다.

## run338H MT5 Runtime Probe(MT5 런타임 탐침)

- run_id(실행 ID): `run338H_execute_runtime_collapsed_onnx_mt5_probe_without_db_v1`
- external_verification_status(외부 검증 상태): `completed(완료)`
- matched_rows(일치 행): `5827`
- mismatch_rows(불일치 행): `0`
- effect(효과): 실제 MT5 실행 또는 차단 사유를 다음 검토로 넘긴다.

## run338I MT5 Probe Review(MT5 탐침 검토)

- run_id(실행 ID): `run338I_review_runtime_collapsed_onnx_mt5_probe_or_repair_without_db_v1`
- MT5 net profit(MT5 순수익): `42.01`
- profit factor(수익 팩터): `2.12`
- trade count(거래수): `11`
- weakness(약점): `recovery_factor_below_1_00;trade_count_below_30;signal_side_short_heavy`
- effect(효과): 양수 단서는 보존하고 운영 승격(operating promotion, 운영 승격)은 막았다.

## run338I MT5 Probe Review(MT5 탐침 검토)

- run_id(실행 ID): `run338I_review_runtime_collapsed_onnx_mt5_probe_or_repair_without_db_v1`
- MT5 net profit(MT5 순수익): `42.01`
- profit factor(수익 팩터): `2.12`
- trade count(거래수): `11`
- weakness(약점): `recovery_factor_below_1_00;trade_count_below_30;signal_side_short_heavy`
- effect(효과): 양수 단서는 보존하고 운영 승격(operating promotion, 운영 승격)은 막았다.

## run338I MT5 Probe Review(MT5 탐침 검토)

- run_id(실행 ID): `run338I_review_runtime_collapsed_onnx_mt5_probe_or_repair_without_db_v1`
- MT5 net profit(MT5 순수익): `42.01`
- profit factor(수익 팩터): `2.12`
- trade count(거래수): `11`
- weakness(약점): `recovery_factor_below_1_00;trade_count_below_30;signal_side_short_heavy`
- effect(효과): 양수 단서는 보존하고 운영 승격(operating promotion, 운영 승격)은 막았다.

## run338J Trade Count Recovery Expansion Package(거래수 회복 확장 패키지)

- run_id(실행 ID): `run338J_materialize_trade_count_recovery_expansion_mt5_probe_package_without_db_v1`
- attempts(시도): `4`
- rows(행): `5827`
- next(다음): `run338K_execute_trade_count_recovery_expansion_mt5_probe_without_db_v1`
- effect(효과): threshold corridor(임계값 구간)를 MT5 runtime probe(MT5 런타임 탐침)로 넘긴다.

## run338K Trade Count Recovery MT5 Probe(거래수 회복 MT5 탐침)

- run_id(실행 ID): `run338K_execute_trade_count_recovery_expansion_mt5_probe_without_db_v1`
- attempts(시도): `4`
- matched_rows(일치 행): `23308/23308`
- best_attempt(최고 시도): `j02_p55_m00`
- effect(효과): threshold corridor(임계값 구간)를 실제 MT5(메타트레이더5) 근거로 바꿨다.

## run338L Trade Count Recovery Review(거래수 회복 검토)

- run_id(실행 ID): `run338L_review_trade_count_recovery_expansion_mt5_probe_without_db_v1`
- best_attempt(최고 시도): `j02_p55_m00`
- net profit(순수익): `70.32`
- trade count(거래수): `21`
- next(다음): `run338M_materialize_lifecycle_exit_side_balance_recovery_expansion_mt5_probe_package_without_db_v1`
- effect(효과): 긍정 단서를 lifecycle/exit(생명주기/청산) 수리로 넘겼다.

## run338L Trade Count Recovery Review(거래수 회복 검토)

- run_id(실행 ID): `run338L_review_trade_count_recovery_expansion_mt5_probe_without_db_v1`
- best_attempt(최고 시도): `j02_p55_m00`
- net profit(순수익): `70.32`
- trade count(거래수): `21`
- next(다음): `run338M_materialize_lifecycle_exit_side_balance_recovery_expansion_mt5_probe_package_without_db_v1`
- effect(효과): 긍정 단서를 lifecycle/exit(생명주기/청산) 수리로 넘겼다.

## run338L Trade Count Recovery Review(거래수 회복 검토)

- run_id(실행 ID): `run338L_review_trade_count_recovery_expansion_mt5_probe_without_db_v1`
- best_attempt(최고 시도): `j02_p55_m00`
- net profit(순수익): `70.32`
- trade count(거래수): `21`
- next(다음): `run338M_materialize_lifecycle_exit_side_balance_recovery_expansion_mt5_probe_package_without_db_v1`
- effect(효과): 긍정 단서를 lifecycle/exit(생명주기/청산) 수리로 넘겼다.

## run338M Lifecycle Exit Package(생명주기 청산 패키지)

- run_id(실행 ID): `run338M_materialize_lifecycle_exit_side_balance_recovery_expansion_mt5_probe_package_without_db_v1`
- attempts(시도): `6`
- next(다음): `run338N_execute_lifecycle_exit_side_balance_recovery_expansion_mt5_probe_without_db_v1`
- effect(효과): lifecycle/exit(생명주기/청산) 변형을 MT5 runtime probe(MT5 런타임 탐침)로 넘긴다.

## run339A Stage Branch(339A 단계 분기)

- branch_run(분기 실행): `run339A_branch_stage338_to_lifecycle_exit_probe_review_without_db_v1`
- new_stage(새 단계): `339_runtime_lifecycle_exit__side_balance_probe_review`
- next_run(다음 실행): `run339B_review_recovered_lifecycle_exit_side_balance_mt5_probe_without_db_v1`
- effect(효과): Stage338(338단계)의 lifecycle/exit(생명주기/청산) runtime review(런타임 검토)를 Stage339(339단계)로 나눠 단계 무게를 줄였다.
