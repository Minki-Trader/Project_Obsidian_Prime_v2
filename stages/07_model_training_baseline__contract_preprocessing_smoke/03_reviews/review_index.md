# Review Index

## 상태(Status, 상태)

`planning_scaffold(계획 골격)`

Stage 07(7단계)는 아직 실행되지 않았다.

## 다음 검토(Next Review, 다음 검토)

첫 검토(review, 검토)는 baseline smoke training(기준선 스모크 학습)의 실행 정체성(identity, 정체성), 측정(measurement, 측정), 판정(judgment, 판정)을 대상으로 한다.
