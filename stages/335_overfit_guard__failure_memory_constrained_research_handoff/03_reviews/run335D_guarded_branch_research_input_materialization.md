# run335D Guarded Branch Research Input Materialization(335D 방어 분기 연구 입력 실체화)

- run_id(실행 ID): `run335D_materialize_guarded_branch_research_inputs_v1`
- status(상태): `completed_guarded_branch_research_inputs_materialized_no_selection`
- judgment(판정): `guarded_branch_inputs_materialized_research_only_no_goal_achieve`
- decision(결정): `stage335D_guarded_branch_inputs_materialized_ready_for_review_no_selection`
- branch_payloads(분기 페이로드): `11`
- failed_gates(실패 게이트): `0`
- next_action(다음 행동): `run335E_review_guarded_branch_input_materialization_v1`
- branch_names(분기 이름): `cost_spread_slippage_grid_guard, curve_noncalendar_state_holdout, direction_symmetry_no_side_drop, drawdown_underwater_recovery_quality, regime_predeclared_macro_state, runtime_identity_strict_handoff, cp322a_exact_blocker_control, cost_curve_drawdown_interaction_guard, regime_direction_interaction_guard, subject_swap_negative_control, null_adjacent_period_control`

Effect(효과): run335C(335C 실행)의 11개 branch design(분기 설계)을 branch payload(분기 페이로드), source binding(원천 연결), negative control(부정 대조), stop condition(중단 조건), tier KPI(티어 핵심 성과 지표), runtime gate(런타임 게이트) 입력으로 고정했다.

Boundary(경계): model training(모델 학습), threshold retuning(임계값 재조정), lot optimization(로트 최적화), direct forward pocket filtering(직접 전진 포켓 필터링), candidate selection(후보 선택), Forward Passed/Failed(전진 통과/실패), runtime authority(런타임 권위), Goal Achieve(목표 달성)는 모두 `not_claimed`다.
