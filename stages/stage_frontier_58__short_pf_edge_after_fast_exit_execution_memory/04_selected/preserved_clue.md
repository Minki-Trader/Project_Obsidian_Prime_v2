# Frontier58 Preserved Clue(전선58 보존 단서)

- judgment(판정): `negative_memory_microstructure_friction_source_did_not_transfer(부정 기억, 미시구조 마찰 원천이 MT5로 전이되지 않음)`
- clue(단서): compressed proxy(압축 프록시)와 all-signal proxy(전체 신호 프록시)를 함께 기록해 MT5 trade density(MT5 거래 밀도)와 직접 비교 가능한 형태를 만들었다.
- clue(단서): validation/OOS runtime density(검증/표본외 런타임 밀도)는 `7.6775956284153` / `9.290076335877863`로 기록됐다.
- clue(단서): F56/F57 memory label(기억 라벨) 대비 Jaccard(자카드)와 feature_ready_diff(피처 준비 차이), signal_diff(신호 차이)를 분리해 source failure(원천 실패)와 handoff failure(인계 실패)를 구분했다.
- boundary(경계): clue only(단서 전용), no authority(권위 없음), no baseline(기준선 없음), no promotion(승격 없음).
