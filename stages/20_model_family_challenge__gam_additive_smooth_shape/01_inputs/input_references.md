# Stage20 Input References(20단계 입력 참조)

## Source Surface(원천 표면)

- data surface(데이터 표면): audited 58-feature MT5 price-proxy surface(감사된 58개 피처 MT5 가격 대리 표면)
- split(분할): Stage03(3단계) label/split contract(라벨/분할 계약)
- work order(작업서): `docs/workspace/stage19_25_model_research_work_order.md`
- prior stage closeout(이전 단계 마감): `stages/19_model_family_challenge__ebm_explainable_boosting_shape/03_reviews/stage19_closeout_packet.md`

효과(effect, 효과): Stage20(20단계)는 Stage19(19단계)의 EBM(설명가능 부스팅 머신) model(모델)이나 threshold(임계값)를 이어받지 않고, 같은 audited input surface(감사된 입력 표면)에서 새 GAM(일반화 가산 모델) 질문을 시작한다.

## Not Yet Materialized(아직 물질화되지 않음)

- GAM(일반화 가산 모델) training run(학습 실행)
- prediction files(예측 파일)
- MT5(`MetaTrader 5`, 메타트레이더5) runtime files(런타임 파일)
- KPI(`Key Performance Indicator`, 핵심 성과 지표) records(기록)

효과(effect, 효과): 이 단계 개방은 planning/opening(계획/개방)만 뜻하며, 결과 주장(result claim, 결과 주장)은 아직 없다.
