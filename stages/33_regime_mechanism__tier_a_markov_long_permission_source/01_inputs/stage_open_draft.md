# Stage Open Draft(단계 개방 초안): Stage33 Tier A Markov Long Permission Source(33단계 티어 A 마르코프 롱 허용 원천)

- user topic(사용자 주제): `Tier A Markov state long permission filter(티어 A 마르코프 상태 롱 허용 필터)` 보존 후 원인 확인
- stage id(단계 ID): `33_regime_mechanism__tier_a_markov_long_permission_source`
- exploration label(탐색 라벨): `stage33_RegimeMechanism__TierA_Markov_LongPermission`
- first planned run(첫 계획 실행): `run27A_tier_a_markov_long_permission_source_scout_v1`
- stage question(단계 질문): Tier A(티어 A) Markov long permission(마르코프 롱 허용)은 왜 validation/OOS(검증/표본외)에서 profit factor(수익 팩터)를 지탱했나?
- named_pattern_policy(이름 붙은 패턴 정책): 먼저 고정 taxonomy(분류표)를 만들지 않고, state/confidence/time/trade-shape(상태/신뢰/시간/거래 형태) 단위에서 반복되는 named pattern(이름 붙은 패턴)을 찾는다.
- candidate_role_boundary(후보 역할 경계): run22B(22B 실행)는 seed clue(씨앗 단서)다. baseline(기준선), promotion(승격), runtime authority(런타임 권위), live readiness(실거래 준비)가 아니다.

## Required Records(필수 기록)

- Tier A separate(Tier A 분리): long-only permission(롱 전용 허용) 중심
- Tier B separate(Tier B 분리): fallback weakness(대체 약점) 비교
- Tier A+B combined(Tier A+B 합산): routed total(라우팅 전체)을 synthetic sum(합성 합산)으로 오해하지 않기

## Initial Evidence(초기 근거)

- Tier A only(Tier A 단독) validation(검증): `net_profit=208.01; pf=1.77; trades=77; short=0`
- Tier A only(Tier A 단독) OOS(표본외): `net_profit=62.65; pf=1.22; trades=51; short=0`
- Tier B fallback only(Tier B 대체 단독): validation/OOS(검증/표본외) `pf=0.86 / 0.99`

효과(effect, 효과): 다음 행동(next action, 다음 행동)은 수익 숫자를 키우는 것이 아니라, 왜 Tier A(티어 A) long permission(롱 허용)이 살아남았는지 설명 가능한 조각으로 자르는 것이다.
