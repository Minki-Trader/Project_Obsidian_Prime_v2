# frontier41A_stage_open_short_pf_edge_exit_shape_source_hypothesis_design_v1 report(보고서)

F41 opens a frozen-entry exit-shape hypothesis(고정 진입 청산 형태 가설). F40 is reference only(참조 전용) and gives entry masks, not winner/baseline/runtime authority(승자/기준선/런타임 권위).

- Grok stage-open classification(그록 단계 개방 분류): `needs_local_verification`
- accepted_after_local_verification(로컬 검증 후 수용): True
- entry freeze guardrail(진입 고정 보호선): True
- same-entry guardrail(동일 진입 보호선): True
- feature_hash_matches_contract(피처 해시 계약 일치): True
- required_splits_present(필수 분할 존재): True

Effect(효과): entry(진입)는 고정하고 exit(청산)만 실험한다.
