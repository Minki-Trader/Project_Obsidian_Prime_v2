# Result Summary

## 판독(Read, 판독)

`20260424_practical_full_cash_freeze_materialization` 실행(run, 실행)은 `positive(긍정)`다.

첫 shared feature frame freeze(공유 피처 프레임 동결 산출물)가 실제로 물질화되었고, 데이터셋 정체성(dataset identity, 데이터셋 정체성)과 해시(hash, 해시)가 남았다.

## 핵심 결과(Key Result, 핵심 결과)

- dataset_id(데이터셋 ID): `dataset_fpmarkets_v2_us100_m5_20220901_20260413_cashopen_fullcash_valid_freeze01`
- selected rows(선택 행 수): `54439`
- scope(범위): `valid_row_only` + `cash_open_rows_only` + `full_cash_session_days_only`
- external verification status(외부 검증 상태): `not_applicable(해당 없음)`

## 경계(Boundary, 경계)

이 실행(run, 실행)은 label definition(라벨 정의), split contract(분할 계약), runtime authority(런타임 권위), operating promotion(운영 승격)을 닫지 않는다.
