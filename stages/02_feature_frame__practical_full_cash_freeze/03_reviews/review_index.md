# Review Index

## 현재 판독(Current Read, 현재 판독)

Stage 02(2단계)는 첫 shared feature frame freeze(공유 피처 프레임 동결 산출물)를 물질화했고, 검토(review, 검토)를 마쳤다.

## 포함된 검토(Included Review, 포함된 검토)

- `first_shared_feature_frame_freeze_review.md`

## 인계(Home of Remaining Work, 남은 작업의 인계처)

남은 일(remaining work, 남은 작업)은 `03_training_dataset__label_split_contract`로 넘긴다. 여기서는 첫 training label(학습 라벨)과 split contract(분할 계약)를 정한다.
