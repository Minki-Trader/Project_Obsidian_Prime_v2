# Frontier56 Preserved Clue(전선56 보존 단서)

- judgment(판정): `negative_memory_adverse_excursion_source_did_not_transfer(부정 기억, 불리 이동 회피 원천이 MT5로 전이되지 않음)`
- clue(단서): short-only routing(숏 전용 라우팅)은 유지됐고, handoff parity(인계 동등성)는 blocker(차단 요인)가 아니었다.
- clue(단서): MT5 trade rate(MT5 거래 빈도)는 proxy filtered trade rate(프록시 필터 거래 빈도)보다 raw signal rate(원신호 빈도)에 가까웠다.
- clue(단서): OOS(표본외)는 validation(검증)보다 덜 나빴지만 edge proof(우위 증거)는 아니다.
- boundary(경계): clue only(단서 전용), no authority(권위 없음), no baseline(기준선 없음), no promotion(승격 없음).
