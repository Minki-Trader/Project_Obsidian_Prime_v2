# 364_source_regime_label_pivot__dense_cost_recovery

Current run(현재 실행): `run364X_execute_density_side_balance_repair_mt5_runtime_probe_without_db_v1`

Latest completed run(최근 완료 실행): `run364W_package_density_side_balance_repair_runtime_probe_without_db_v1`

Current truth(현재 진실): density side-balance repair candidate(밀도 방향 균형 수리 후보)가 MT5 runtime package(MT5 런타임 패키지)로 준비됐다.

Next action(다음 행동): run364X MT5 runtime probe(MT5 런타임 탐침) 실행.
