# Stage364 selection status(선택 상태)

Updated(갱신): 2026-06-04T21:20:30Z

Current run(현재 실행): `run364CJ_train_h17_focus_month_cost_stress_repair_scout_without_db_v1`
Latest completed run(최근 완료 실행): `run364CI_materialize_h17_focus_month_cost_stress_repair_inputs_without_db_v1`

Package candidate(패키지 후보): none(없음). CI is materialization only(CI는 구체화 전용).

Materialized queue(구체화 대기열): `stages/364_source_regime_label_pivot__dense_cost_recovery/02_runs/run364CI/run364CJ_h17_focus_month_cost_stress_repair_scout_queue.csv` with `16` rows(행).

Reviewed seed(검토 씨앗): `cg09_best_open_hour_overlay_focus`. Reviewed KPI(검토 핵심 성과 지표): net `1001.5`, PF `1.3999745705`, density `3.2070063694`, shorts `104`.

Guardrails(가드레일): top_n rows(top_n 행) `0`, trade splitting rows(거래 쪼개기 행) `0`, exact date filter rows(정확 날짜 필터 행) `0`.

Authority(권위): runtime authority(런타임 권위), operating promotion(운영 승격), live readiness(실거래 준비), Goal Achieve(목표 달성)는 모두 not claimed(주장 안 함).
