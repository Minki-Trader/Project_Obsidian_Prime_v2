# run285A Final Candidate Package Report(최종 후보 패키지 보고)

- stage_id(단계 ID): `285_onnx_candidate_campaign__onnx_export_parity_runtime_reproduction_cp282d`
- run_id(실행 ID): `run285A_export_cp282d_adapter_to_onnx_and_runtime_reproduction_v1`
- selected_candidate(선택 후보): `cp282D_macro_trend_countercheck_surface`
- Adapter package(어댑터 패키지): `stage283_cp282d_macro_trend_countercheck_adapter_package_v1`
- status(상태): `completed_cp282d_onnx_export_parity_runtime_reproduction_package_ready_for_main_push`
- judgment(판정): `onnx_export_parity_and_mt5_runtime_reproduction_passed_main_push_pending`
- ONNX model(온엑스 모델): `stages/285_onnx_candidate_campaign__onnx_export_parity_runtime_reproduction_cp282d/02_runs/run285A/models/cp282d_route_signal_identity.onnx`
- feature_order_hash(피처 순서 해시): `171b545c231c11df3686e77d62d930c01807dc1165c49fb5e96bbb1bf97ed22d`
- Python inference check(파이썬 추론 확인): `stages/285_onnx_candidate_campaign__onnx_export_parity_runtime_reproduction_cp282d/02_runs/run285A/python_inference_check.json`
- ONNX parity receipt(온엑스 동등성 영수증): `stages/285_onnx_candidate_campaign__onnx_export_parity_runtime_reproduction_cp282d/02_runs/run285A/onnx_parity_receipt.json`
- MT5 runtime reproduction(MT5 런타임 재현): `completed`
- attempts(시도): `6/6`
- runtime_metric_comparison(런타임 지표 비교): `36` rows(행), `0` failed(실패)
- Goal Achieve(목표 달성): `complete_pending_main_push`
- next_action(다음 행동): `commit_and_push_main_then_mark_goal_achieved`

## Package Meaning(패키지 의미)

cp282D(282D 후보)는 route_signal_value(경로 신호값) 하나를 ONNX probability graph(온엑스 확률 그래프)로 넘기는 Adapter candidate package(어댑터 후보 패키지)다.
Effect(효과): feature order(피처 순서), decision surface(판단 표면), risk logic(위험 로직), runtime handoff(런타임 인계)를 같은 경로에서 추적할 수 있다.

## Boundary(경계)

`research_development_only_no_live_readiness_no_runtime_authority_no_operating_promotion_no_operating_reference_no_production_baseline_no_deployment_final_package_for_onnx_research_handoff_only`

Effect(효과): 이 보고서는 ONNX package handoff(온엑스 패키지 인계) 근거이며 live readiness(실거래 준비), runtime authority(런타임 권위), operating promotion(운영 승격), deployment(배포)를 주장하지 않는다.
