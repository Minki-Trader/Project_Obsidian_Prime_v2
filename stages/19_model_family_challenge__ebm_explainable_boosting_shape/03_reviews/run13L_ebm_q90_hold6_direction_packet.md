# run13L_ebm_q90_hold6_direction_probe_v1 Result Summary(run13L 결과 요약)

- topic read(주제 판독): `q90_hold6_long_short_direction_asymmetry`
- selected variant(선택 변형): `v01_main_effects_broad_bins`
- threshold(임계값): `q0.90`
- max hold bars(최대 보유 봉): `6`
- judgment(판정): `inconclusive_ebm_q90_hold6_direction_asymmetry_runtime_probe_completed`
- external verification(외부 검증): `completed`
- MT5 KPI records(MT5 핵심 성과 지표 기록): `20`
- normalized KPI records(정규화 핵심 성과 지표 기록): `20`

## Runtime Read(런타임 판독)

| split(분할) | net profit(순수익) | profit factor(수익 팩터) | trades(거래 수) | max DD(최대 손실) | recovery(회복 계수) |
|---|---:|---:|---:|---:|---:|
| validation(검증) | `73.58` | `1.1` | `196` | `274.69` | `0.27` |
| OOS(표본외) | `18.7` | `1.03` | `165` | `106.44` | `0.18` |

효과(effect, 효과): 이 run(실행)은 EBM(설명가능 부스팅 머신) shape clue(모양 단서)를 MT5(메타트레이더5) runtime probe(런타임 탐침)로 관찰한다.

Forbidden claims(금지 주장): edge(거래 우위), alpha quality(알파 품질), baseline(기준선), promotion(승격), runtime authority(런타임 권위).

## Direction Split(방향 분리)

- long validation/OOS trades(매수 검증/표본외 거래 수): `196` / `165`
- short validation/OOS trades(매도 검증/표본외 거래 수): `111` / `88`
