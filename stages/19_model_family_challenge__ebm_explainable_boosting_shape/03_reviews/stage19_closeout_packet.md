# Stage19 Closeout Packet(19단계 종료 묶음)

## Judgment(판정)

- stage(단계): `19_model_family_challenge__ebm_explainable_boosting_shape`
- status(상태): `closed_inconclusive_ebm_model_characteristics_exhausted`
- result subject(결과 대상): EBM(`Explainable Boosting Machine`, 설명가능 부스팅 머신) explainable boosting shape(설명 가능한 부스팅 모양) model-family scout(모델군 탐색)
- claim boundary(주장 경계): `runtime_probe_and_model_characteristic_read_only_not_edge_not_alpha_quality_not_baseline_not_promotion_not_runtime_authority`
- selected baseline/promotion/runtime authority(선택 기준선/승격/런타임 권위): `none(없음)`

효과(effect, 효과): Stage19(19단계)은 EBM(설명가능 부스팅 머신) 특성을 충분히 확인했지만, 운영 의미(operating meaning, 운영 의미)는 만들지 않고 닫는다.

## Evidence(근거)

- structural packet(구조 묶음): `docs/agent_control/packets/stage19_run13A_ebm_shape_scout_v1/aggregate_summary.json`
- runtime batch packet(런타임 묶음): `docs/agent_control/packets/stage19_ebm_run13B_run13G_mt5_runtime_batch_v1/aggregate_summary.json`
- attribution packet(귀속 묶음): `docs/agent_control/packets/stage19_run13H_ebm_feature_hold6_routing_attribution_v1/run_summaries/run13H_ebm_feature_hold6_routing_attribution_v1.json`
- deep axis packet(심층 축 묶음): `docs/agent_control/packets/stage19_run13M_ebm_deep_axis_followup_v1/run_summaries/run13M_ebm_deep_axis_followup_v1.json`
- axis extension packet(축 확장 묶음): `docs/agent_control/packets/stage19_run13T_ebm_mt5_axis_extension_v1/run_summaries/run13T_ebm_mt5_axis_extension_v1.json`
- axis exhaustion packet(축 소진 묶음): `docs/agent_control/packets/stage19_run13AD_ebm_axis_exhaustion_followthrough_v1/run_summaries/run13AD_ebm_axis_exhaustion_followthrough_v1.json`
- registered runs(등록 실행): `32`
- stage ledger rows(단계 장부 행): `430`
- MT5/trading ledger rows(MT5/거래 장부 행): `330`

효과(effect, 효과): Python(파이썬) structural scout(구조 탐색), MQL5(엠큐엘5) score table(점수표), MT5(`MetaTrader 5`, 메타트레이더5) strategy tester(전략 테스터), KPI(`Key Performance Indicator`, 핵심 성과 지표)를 같은 종료 근거로 묶었다.

## Preserved Clues(보존 단서)

- EBM(설명가능 부스팅 머신)은 MQL5(엠큐엘5) score table(점수표)로 직접 실행 가능하다.
- `ema50_ema200_diff`는 가장 큰 단일 feature dependency(피처 의존성)다.
- hold4(4봉 보유) OOS(표본외) 표면은 보였지만 validation(검증) 충돌이 남는다.
- `B_mixed_partial_context`는 Tier B fallback(티어 B 대체) subtype(하위유형) 중 가장 효율적이었다.
- q90 long bias(q90 매수 편향)와 mixed subtype(혼합 하위유형) 조합은 follow-up(후속 탐침) 중 가장 볼만했다.

효과(effect, 효과): Stage20(20단계)은 이 단서를 comparison context(비교 문맥)로만 쓰고, Stage19(19단계) 모델을 기준선으로 상속하지 않는다.

## Negative Memory(부정 기억)

- top5 feature mask(상위 5개 피처 마스크)는 OOS(표본외) 성과를 크게 무너뜨려 feature dependence(피처 의존성)가 취약함을 보였다.
- `ema50_ema200_diff`만 남겨도 baseline(기준선)을 회복하지 못했다.
- q92/q95 direction compression(q92/q95 방향 압축)은 OOS(표본외) net(순손익)을 너무 빨리 잃었다.
- Stage19(19단계)의 어떤 표면도 edge(거래 우위), alpha quality(알파 품질), baseline(기준선), promotion(승격), runtime authority(런타임 권위)에 해당하지 않는다.

효과(effect, 효과): 좋은 부분 숫자를 이유로 Stage19(19단계)을 운영 의미나 Stage20(20단계) baseline(기준선)으로 끌고 가지 않는다.

## Closeout Rule(종료 규칙)

Stage20(20단계)는 EBM(설명가능 부스팅 머신) continuation(연속) 단계가 아니다. Stage19(19단계)의 model(모델), threshold(임계값), selected variant(선택 변형), runtime files(런타임 파일)는 Stage20(20단계)에 baseline(기준선)으로 상속하지 않는다.

효과(effect, 효과): Stage20(20단계)은 GAM(`Generalized Additive Model`, 일반화 가산 모델) smooth additive shape(부드러운 가산 모양)라는 새 model-family question(모델군 질문)으로 시작한다.
