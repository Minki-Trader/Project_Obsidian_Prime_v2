# Frontier59 Preserved Clue(전선59 보존 단서)

- judgment(판정): `negative_memory_long_axis_did_not_escape_friction_class(부정 기억, 롱 축이 마찰/경제성 붕괴 계열을 벗어나지 못함)`
- clue(단서): long-only p_long handoff(롱 전용 p_long 인계)를 만들어 MT5 signal_diff(신호 차이)와 feature_ready_diff(피처 준비 차이)를 직접 비교했다.
- clue(단서): validation/OOS runtime density(검증/표본외 런타임 밀도)는 `5.475409836065574` / `5.251908396946565`로 기록됐다.
- clue(단서): economics stress(경제성 압박)와 trade shape(거래 형태)를 proxy row(프록시 행)에 붙여 MT5 붕괴가 비용 민감성인지 볼 수 있게 했다.
- clue(단서): technical transfer path(기술 전이 경로)와 density fidelity(밀도 충실도)는 작동했지만 PnL fidelity(손익 충실도)는 무너졌다는 분리를 보존한다.
- boundary(경계): clue only(단서 전용), no authority(권위 없음), no baseline(기준선 없음), no promotion(승격 없음).
