# run337IJ Runtime Probe Package(run337IJ 런타임 탐침 패키지)

## Summary(요약)

- run_id(실행 ID): `run337IJ_materialize_runtime_positive_low_pf_drawdown_side_balance_repair_runtime_probe_package_without_db_v1`
- parent_run_id(부모 실행 ID): `run337II_review_runtime_positive_low_pf_drawdown_side_balance_repair_training_without_db_v1`
- judgment(판정): `runtime_probe_package_ready_for_weak_proxy_positive_candidate_proxy_mt5_diff_required_no_selection`
- gates(게이트): `12/12`
- attempt_rows(시도 행): `1`
- feature_matrix_rows(피처 행렬 행): `5841`
- expected_probability_rows(예상 확률 행): `5841`
- candidate_model_ids(후보 모델 ID): `ih_if_ie003_pf_recovery_fwd18_xgboost`
- common_sync(공용 파일 동기화): `4/4`

## Action(행동)

II의 weak proxy-positive(약한 프록시 양성) ONNX(온엑스) 후보 1개를 MT5 runtime probe package(런타임 탐침 패키지)로 만들었다.
Effect(효과): feature matrix(피처 행렬), expected tape(예상 테이프), ONNX(온엑스), set/ini(설정 파일)가 같은 lineage(계보) 안에서 비교 가능해졌다.

## Boundary(경계)

IJ는 package(패키지)만 만들었다. MT5 execution(MT5 실행), candidate selection(후보 선택), runtime authority(런타임 권위), Goal Achieve(목표 달성)는 없다.

## Next(다음)

`run337IK_execute_runtime_positive_low_pf_drawdown_side_balance_repair_mt5_runtime_probe_without_db_v1`에서 MT5 runtime probe(런타임 탐침)를 실행한다.
Effect(효과): proxy expected value(프록시 예상값)와 MT5 runtime output(런타임 출력)의 diff(차이)를 확인한다.
