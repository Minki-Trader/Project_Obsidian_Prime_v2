# run337JI Runtime Negative Collapse Repair MT5 Runtime Probe(run337JI 런타임 음수 붕괴 수리 MT5 런타임 탐침)

## Summary(요약)

- run_id(실행 ID): `run337JI_execute_runtime_negative_collapse_cost_stress_trade_shape_repair_mt5_runtime_probe_without_db_v1`
- parent_run_id(부모 실행 ID): `run337JH_materialize_runtime_negative_collapse_cost_stress_trade_shape_repair_runtime_probe_package_without_db_v1`
- status(상태): `completed_stage337JI_runtime_negative_collapse_repair_mt5_runtime_probe_executed_review_required_no_forward_decision`
- judgment(판정): `mt5_runtime_probe_outputs_available_proxy_diff_review_required_no_selection`
- gates(게이트): `8/8`
- attempts(시도): `2`
- runtime_completed_rows(런타임 완료 행): `2`
- matched_rows(일치 행): `11682`
- mismatch_rows(불일치 행): `0`
- net_profit(순수익): `202.81`
- profit_factor(수익 팩터): `1.1`
- trade_count(거래수): `859`
- blocker(차단 사유): ``

## Action(행동)

JH package(JH 패키지)의 두 ONNX(온엑스) 후보를 MT5 runtime probe(MT5 런타임 탐침)로 실행하거나 차단 사유를 기록했다.
Effect(효과): proxy expected value(프록시 예상값)가 MT5 output(MT5 출력) 또는 blocker(차단 사유)와 연결된다.

## Boundary(경계)

이번 실행은 runtime probe attempt(런타임 탐침 시도)만 뜻한다. Candidate selection(후보 선택), Forward Passed/Failed(전진 통과/실패), runtime authority(런타임 권위), Goal Achieve(목표 달성)는 없다.
