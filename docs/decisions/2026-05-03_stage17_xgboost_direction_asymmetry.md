# 2026-05-03 Stage17 XGBoost Direction Asymmetry(17단계 XGBoost 방향 비대칭)

## Decision(결정)

`run11C_xgb_q80_direction_asymmetry_probe_v1`를 q0.80 long-only/short-only(롱 전용/숏 전용) direction asymmetry probe(방향 비대칭 탐침)로 실행했다.

효과(effect, 효과): run11B(실행11B)의 성급한 closeout(마감)을 대체하고, 새 특성이 보이면 Stage17(17단계)을 계속 열어둔다.

## Judgment(판정)

- judgment(판정): `inconclusive_xgboost_direction_asymmetry_runtime_probe_completed`
- recommendation(권고): `keep_stage17_open_for_trade_shape_or_regime_attribution`
- boundary(경계): `xgboost_direction_asymmetry_runtime_probe_only_not_edge_not_alpha_quality_not_baseline_not_promotion_not_runtime_authority`
