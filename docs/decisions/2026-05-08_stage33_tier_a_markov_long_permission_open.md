# Decision(결정): Stage33 Tier A Markov Long Permission Open(33단계 티어 A 마르코프 롱 허용 개방)

Stage33(33단계) `33_regime_mechanism__tier_a_markov_long_permission_source`를 open-only(개방만) 상태로 연다.

## Why(이유)

Stage28(28단계) run22B(22B 실행)에서 Tier A only(Tier A 단독)는 validation/OOS(검증/표본외) 모두 long-only(롱 전용)로 positive net(양의 순손익)을 보였지만, Tier B fallback only(Tier B 대체 단독)는 약했다.

## First Planned Run(첫 계획 실행)

`run27A_tier_a_markov_long_permission_source_scout_v1`는 structural attribution scout(구조 귀속 탐침)이다.

## Boundary(경계)

Stage33(33단계)는 seed clue(씨앗 단서)를 해부하는 단계다. baseline(기준선), promotion(승격), runtime authority(런타임 권위), live readiness(실거래 준비)는 아니다.

효과(effect, 효과): 다음 작업은 Markov regression(마르코프 회귀) 전체를 더 미는 것이 아니라, Tier A(티어 A) long permission(롱 허용)이 어느 상태/시간/거래 형태에서 살아났는지 확인한다.
