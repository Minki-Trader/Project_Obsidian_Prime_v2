# Stage364V density side-balance ONNX scout(Stage364V 밀도 방향 균형 온엑스 탐색)

## Current truth(현재 진실)

- run_id(실행 ID): `run364V_train_density_side_balance_repair_onnx_scout_without_db_v1`
- parent_run_id(부모 실행 ID): `run364U_materialize_density_side_balance_repair_inputs_without_db_v1`
- next_run_id(다음 실행 ID): `run364W_package_density_side_balance_repair_runtime_probe_without_db_v1`
- judgment(판정): `positive_proxy_runtime_executable_candidate_density_side_balance_repaired_mt5_probe_required_no_authority`
- claim_boundary(주장 경계): `research_development_onnx_threshold_scout_and_shadow_training_only_no_mt5_execution_no_forward_pass_no_live_readiness_no_operating_promotion_no_runtime_authority_no_goal_claim`

## Action/Effect(행동/효과)

Action(행동): existing ONNX probabilities(기존 온엑스 확률)에 short threshold(숏 임계값), ADX long block(ADX 롱 차단), max hold(최대 보유)를 조합해 runtime-executable dual-side surface(런타임 실행 가능 양방향 표면)를 만들었다. Shadow loss guard ONNX(그림자 손실 방어 온엑스)도 학습했지만 현재 EA primary output(현재 전문가 자문 주 출력)과 맞지 않아 선택하지 않았다.

Effect(효과): long-only failure(롱 전용 실패)를 MT5 package(MT5 패키지)로 검증 가능한 후보로 바꿨고, binary shadow model(2출력 그림자 모델)은 나중에 EA capability(EA 기능)가 맞을 때만 재사용한다.

## Selected candidate(선택 후보)

- variant(변형): `dual_pshort_0_45__adx_block_40_0__maxhold_8`
- short threshold(숏 임계값): `0.45`
- min margin(최소 마진): `-0.000562137088`
- ADX long block min(ADX 롱 차단 최소값): `40.0`
- max hold M5(최대 보유 5분봉): `8`
- validation/OOS/combined density(검증/OOS/합산 밀도): `3.0721649485` / `3.4892086331` / `3.2462462462`
- validation/OOS/combined net(검증/OOS/합산 순수익): `270.552` / `501.012` / `771.564`
- combined PF/expectancy/DD/RF(합산 수익 팩터/기대값/낙폭/회복 계수): `1.2218406503` / `0.7137502313` / `-155.007` / `4.9776074629`
- long/short count(롱/숏 거래수): `952` / `129`

## Evidence(근거)

- dual side surface(양방향 표면): `stages/364_source_regime_label_pivot__dense_cost_recovery/02_runs/run364V/dual_side_runtime_surface.csv`
- selected probability tape(선택 확률 기록): `stages/364_source_regime_label_pivot__dense_cost_recovery/02_runs/run364V/expected_tapes/dual_side_selected_expected_probability_tape.csv`
- selected trade tape(선택 거래 기록): `stages/364_source_regime_label_pivot__dense_cost_recovery/02_runs/run364V/expected_tapes/dual_side_selected_expected_trade_tape.csv`
- shadow ONNX smoke(그림자 온엑스 스모크): `stages/364_source_regime_label_pivot__dense_cost_recovery/02_runs/run364V/shadow_loss_guard_onnx_smoke_report.csv`
- selected candidate(선택 후보): `stages/364_source_regime_label_pivot__dense_cost_recovery/02_runs/run364V/selected_runtime_candidate.json`

## Boundary(경계)

이 결과는 proxy expected value(프록시 예상값)다. MT5 execution(MT5 실행)은 아직 `not_run`이고, Goal Achieve(목표 달성), operating promotion(운영 승격), runtime authority(런타임 권위), live readiness(실거래 준비)는 모두 `not_claimed`다.
