# Stage304 Decision(304단계 결정)

- decision(결정): `actual_mt5_no_onnx_worthy_candidate_runtime_realized_curve_rebuild_opened`
- selected_candidate(선택 후보): `none`
- Adapter package(어댑터 패키지): `none`
- ONNX readiness(온엑스 준비): `not_started`
- next_stage(다음 단계): `305_onnx_candidate_campaign__runtime_realized_curve_attribution_rebuild`

Effect(효과): 실제 MT5(메타트레이더5) report source_path(보고서 원천 경로)를 이용해 곡선 판정을 복구하고, 후보가 없으면 새 런타임 기여도 재구성으로 넘긴다.
