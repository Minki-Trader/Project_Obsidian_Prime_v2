# Stage303 Decision(302단계 결정)

- decision(결정): `actual_mt5_oos_scale_validation_damage_not_onnx_worthy_no_adapter_no_onnx`
- selected_candidate(선택 후보): `none`
- Adapter package(어댑터 패키지): `none`
- ONNX readiness(온엑스 준비): `not_started`
- next_stage(다음 단계): `304_onnx_candidate_campaign__curve_pocket_aware_profit_source_rebuild`

Effect(효과): OOS scale(표본외 규모) 단서는 보존하지만 ONNX-worthy(온엑스 가치 있음) 후보로 선언하지 않고, regime-balanced router(레짐 균형 라우터) 새 단계로 넘긴다.
