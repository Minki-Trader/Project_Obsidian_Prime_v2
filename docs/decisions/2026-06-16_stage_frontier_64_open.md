# Decision: Open Frontier64(F64 개방 결정)

Action(행동): F63 negative memory(부정 기억) 뒤 `stage_frontier_64__independent_pf_source_after_inverse_signal_memory`를 열었다.

Effect(효과): F61~F63 side allocation(방향 배분) 계열을 repair(수리)하지 않고, loss-cluster hazard admission source(손실 군집 위험 진입 허용 원천)를 새 hypothesis(가설)로 시험한다.

- trigger(계기): F63 MT5 runtime probe(MT5 런타임 탐침) PF(수익 팩터) `0.35 / 0.44`와 DD(손실폭) `22.56% / 15.61%`.
- Grok review(그록 검토): `docs/agent_control/grok_reviews/2026-06-16_frontier64_stage_open/small_review`, classification(분류) `accepted(수용)`.
- next_run(다음 실행): `frontier64B_loss_cluster_hazard_proxy_scout_v1`.
- boundary(경계): exploration-only(탐색 전용); completion/baseline/promotion/runtime authority/live readiness/Goal Achieve(완성/기준선/승격/런타임 권위/실거래 준비/목표 달성)는 not_claimed(주장 없음).
