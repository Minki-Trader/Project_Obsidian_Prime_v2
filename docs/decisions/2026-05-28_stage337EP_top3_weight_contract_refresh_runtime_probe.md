# Decision(결정): Stage337EP Top3 Contract Repaired Runtime Parity Passed(상위3 계약 수리 런타임 동등성 통과)

- stage(단계): `337_onnx_research_packet__cost_buffer_direction_curve_rebuild`
- run(실행): `run337EP_refreshed_forward_surface_runtime_probe_or_failure_memory_without_db_v1`
- parent_run(부모 실행): `run337EO_refresh_survivor_feature_handoff_and_surface_reprobe_without_db_v1`
- decision(결정): `stage337EP_open_run337EQ_forward_kpi_attribution_cost_stress_curve_pocket`
- next_action(다음 행동): `run337EQ_forward_kpi_attribution_cost_stress_curve_pocket_without_db_v1`
- report(보고서): `stages/337_onnx_research_packet__cost_buffer_direction_curve_rebuild/03_reviews/run337EP_top3_weight_contract_refresh_runtime_probe.md`

## Basis(근거)

run337EP(337EP 실행)는 2026-05 top3 monthly proxy weight(월간 상위3 대리 가중치)를 `2026-04-30T23:00:00+00:00` 기준 closed source bar(닫힌 원천 봉)에서 만들었다. no_lookahead_passed(미래 참조 없음 통과)는 `true`이고, macro58/technical42(매크로58/기술42) feature frame(피처 프레임)은 각각 `2026-05-28T06:00:00+00:00` / `2026-05-28T06:00:00+00:00`까지 유효하다.

MT5 argmax runtime parity(MT5 최대확률 런타임 동등성)는 matched_rows(일치 행) `1932`, probability_mismatch_rows(확률 불일치 행) `0`, decision_mismatch_rows(결정 불일치 행) `0`다.

## Decision Boundary(결정 경계)

이 결정은 forward passed(전진 통과)가 아니다. profit/PF/DD/recovery/expectancy(손익/수익 팩터/낙폭/회복/기대값), D/B source attribution(D/B 원천 귀속), long/short attribution(매수/매도 귀속), regime slices(국면 절편), lot-normalized result(랏 정규화 결과), spread/slippage stress(스프레드/슬리피지 스트레스), curve pocket(곡선 포켓)을 아직 닫지 않았다.

## Closeout(종료)

- Forward Passed(전진 통과): `not_claimed`
- Forward Failed(전진 실패): `not_claimed`
- Forward Blocked(전진 차단): `not_claimed`
- runtime_authority(런타임 권위): `not_claimed`
- operating_promotion(운영 승격): `not_claimed`
- deployment(배포): `not_claimed`
- goal_achieve(목표 달성): `not_claimed`

Effect(효과): 데이터/피처/런타임 동등성 공백은 좁혔고, 다음 EQ 실행에서 frozen forward KPI attribution(고정 전진 성과 귀속)과 cost/curve stress(비용/곡선 스트레스)를 판단한다.
