# F90C Decision Memo(F90C 결정 메모)

Decision(결정): Record F90C as negative ordering proxy memory(부정 순서 프록시 기억) and plan `frontier90D_time_to_barrier_repair_or_rotation_decision_v1` for repair or rotation(수리 또는 회전).

Reason(이유): Best diagnostic variant(진단상 최선 변형) `ridge_signed_speed_alpha10` did not preserve validation/OOS(검증/표본외) signal quality enough to create a meaningful signal(의미 있는 신호) or runnable decision surface(실행 가능 의사결정 표면).

Forbidden(금지): candidate(후보), selected baseline(선택 기준선), operating promotion(운영 승격), runtime authority(런타임 권위), live readiness(실거래 준비), Goal Achieve(목표 달성).
