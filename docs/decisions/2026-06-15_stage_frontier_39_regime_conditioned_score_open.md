# Decision: Open Frontier39 Regime Conditioned Score(결정: 전선39 체제 조건화 점수 개방)

Date(날짜): 2026-06-15

Decision(결정): `frontier39A_stage_open_short_pf_edge_regime_conditioned_score_hypothesis_design_v1` starts `stage_frontier_39__short_pf_edge_regime_conditioned_score_after_f38_scout_only`.

Action(행동): F38 score clue(점수 단서)를 reference-only(참조 전용)로 쓰고, train-only regime gate(학습 전용 체제 게이트)를 paired ablation(쌍대 소거)로 시험한다.

Effect(효과): 같은 shallow score repair(얕은 점수 수리)를 반복하지 않고, regime conditioning(체제 조건화)의 실제 기여를 분리한다.
