# Current working state(현재 작업 상태)

date(날짜): 2026-06-02

stage(단계): `364_source_regime_label_pivot__dense_cost_recovery`

current_run_id(현재 실행 ID): `run364W_package_density_side_balance_repair_runtime_probe_without_db_v1`

latest_completed_run_id(최근 완료 실행 ID): `run364V_train_density_side_balance_repair_onnx_scout_without_db_v1`

current_truth(현재 진실): `run364V`는 existing ONNX probabilities(기존 온엑스 확률)를 그대로 쓰고, short threshold(숏 임계값) `0.45`, ADX long block(ADX 롱 차단) `40.0`, maxhold(최대보유) `8` 조합을 next MT5 package(다음 MT5 패키지) 후보로 골랐다. proxy combined net/PF(프록시 합산 순수익/수익 팩터)는 `771.564` / `1.2218406503`이고 long/short(롱/숏)는 `952` / `129`다.

next_action(다음 행동): `run364W_package_density_side_balance_repair_runtime_probe_without_db_v1`에서 selected candidate(선택 후보)를 Common Files(공용 파일), set file(설정 파일), expected tape(예상 기록)로 package(패키지)한다.

claim_boundary(주장 경계): `research_development_onnx_threshold_scout_and_shadow_training_only_no_mt5_execution_no_forward_pass_no_live_readiness_no_operating_promotion_no_runtime_authority_no_goal_claim`
