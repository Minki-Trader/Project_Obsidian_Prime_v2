# Current Working State(현재 작업 상태)

Updated(갱신): 2026-06-04T18:02:05Z

Active stage(활성 단계): `364_source_regime_label_pivot__dense_cost_recovery`

Latest completed run(최근 완료 실행): `run364BX_overlay_hour17_native_short_ablation_runtime_probe_without_db_v1`

Current run(현재 실행): `run364BY_review_overlay_hour17_native_short_ablation_runtime_probe_without_db_v1`

Current truth(현재 진실): `run364BX` completed MT5 runtime ablation(MT5 런타임 제거 비교) for hour17 overlay(17시 오버레이), native-short-only control(기본 숏 단독 대조), and weak late-session firewall(약한 후반 세션 방화벽). Best variant(최선 변형)는 `bx03_hour17_overlay_plus_weak_late_session_firewall`이고 MT5 net/PF/trades(순수익/수익 팩터/거래수)는 `1008.18` / `1.4` / `1008`이다.

Next action(다음 행동): `run364BY_review_overlay_hour17_native_short_ablation_runtime_probe_without_db_v1`에서 BX MT5 output(MT5 출력)을 source/session/month/equity attribution(원천/세션/월/수익곡선 귀속)으로 review(검토)한다.

Operating boundary(운영 경계): runtime authority(런타임 권위), operating promotion(운영 승격), live readiness(실거래 준비), Goal Achieve(목표 달성)는 모두 not claimed(주장 안 함).
