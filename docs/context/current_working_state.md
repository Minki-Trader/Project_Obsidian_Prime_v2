# Current Working State(현재 작업 상태)

- current_stage_id(현재 단계 ID): `364_source_regime_label_pivot__dense_cost_recovery`
- current_run_id(현재 실행 ID): `run364F_prepare_timestamp_context_onnx_runtime_probe_without_db_v1`
- latest_completed_run_id(최근 완료 실행 ID): `run364E_train_timestamp_context_cost_filter_model_without_db_v1`
- current_status(현재 상태): `completed_stage364E_cost_filter_model_trained_onnx_exported_probe_preparation_opened_no_mt5`
- current_judgment(현재 판정): `positive_model_training_onnx_exportable_research_candidate_for_runtime_probe_no_operating_claim`
- current_decision(현재 결정): `stage364E_open_run364F_prepare_timestamp_context_onnx_runtime_probe_without_db_v1`
- claim_boundary(주장 경계): `research_development_model_training_and_onnx_export_only_no_mt5_execution_no_forward_pass_no_live_readiness_no_operating_promotion_no_runtime_authority_no_goal_claim`

Action(행동): Stage364E(364E 실행)가 timestamp context cost-filter model(시점 문맥 비용 필터 모델)을 학습하고 `rf_depth3_balanced` ONNX(온엑스) 스모크를 통과시켰다.

Effect(효과): 다음 작업은 `run364F_prepare_timestamp_context_onnx_runtime_probe_without_db_v1`에서 ONNX artifact(ONNX 산출물), threshold(임계값), feature order(피처 순서)를 MT5 runtime probe(MT5 런타임 탐침)로 인계하는 것이다.
