You are Grok as external second opinion(외부 2차 의견) for Project Obsidian Prime v2.

Review size(검토 크기): small(소규모).
Task(작업): Frontier 53 pre-MT5 runtime probe(MT5 런타임 탐침 전) critique only.

Current truth(현재 진실):
- F53 stage-open(단계 개방) direction was accepted as a new PF source search(수익 팩터 원천 탐색), not F52 lifecycle-only repair(생명주기 전용 수리 반복).
- Codex local verification(로컬 검증) found the scout evidence(탐색 근거) corresponds to logreg_l2_c05 with class_weight balanced(균형 클래스 가중치). The F53 script(실행 파일) was corrected before MT5.
- No completion/baseline/promotion/runtime authority/live readiness/Goal Achieve(완성/기준선/승격/런타임 권위/실거래 준비/목표 달성) is claimed.

Candidate identity(후보 정체성):
- stage_id(단계 ID): stage_frontier_53__short_pf_edge_pf_source_after_runtime_dd_compression_memory
- run_id(실행 ID): frontier53Z_runtime_probe_backfill_v1
- candidate_id(후보 ID): f53b_logreg_l2_c05_short_q25_q70_s90
- model(모델): logreg_l2_c05_balanced(균형 로지스틱 L2 C0.5)
- side(방향): short(숏)
- labels(라벨): train-only MFE >= q70, MAE <= q25, horizon_pnl > 0
- score gate(점수 게이트): train score_q=0.90, threshold=0.5617501169006113
- ONNX parity(온엑스 동등성): passed, rows=4096, max_abs_diff=8.29997136631011e-07
- feature count/hash(피처 수/해시): 58 / fa06973c24462298ea38d84528b07ca0adf357e506f3bfeea02eb0d5691ab8e2

Proxy evidence(프록시 근거):
- train PF/DD/density(학습 수익 팩터/손실폭/밀도): 1.045958 / 13.924099 / 5.1012 per day
- validation_is proxy PF/DD/density(검증 내부 프록시 수익 팩터/손실폭/밀도): 1.001867 / 7.960459 / 7.2568 per day
- OOS proxy PF/DD/density(표본외 프록시 수익 팩터/손실폭/밀도): 1.096191 / 7.350606 / 10.2366 per day
- Stress score_q=0.93(압박 점수 분위수 0.93) stayed weak and lower density, so it is not the first MT5 probe(MT5 탐침) candidate.

Runtime policy(런타임 정책):
- Keep F52 DD clue(F52 손실폭 단서): close-on-flat(무신호 청산), maxhold=6, ATR SL/TP(평균진폭 손익절) enabled with stop 0.8 and TP 1.2 multipliers.
- Remove F52 suppression(F52 억제 제거): entry-transition-only=false, reentry cooldown=0, same-direction cooldown=0.
- ONNX output contract(온엑스 출력 계약): p_short=binary_event_score, p_flat=0, p_long=0, so EA threshold_margin(문턱값 마진) uses the score threshold directly.

Question(질문):
1. Is this candidate and runtime policy valid to send to one MT5 runtime probe(MT5 런타임 탐침)?
2. What local failure mode(로컬 실패 양상) should Codex watch first?
3. Answer bounded as accepted/rejected/needs_local_verification(수용/거절/로컬 검증 필요). Do not claim completion(완성), baseline(기준선), promotion(승격), runtime authority(런타임 권위), live readiness(실거래 준비), or Goal Achieve(목표 달성).
