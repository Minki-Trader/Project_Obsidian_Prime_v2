Codex direction before Grok(그록 전 Codex 방향):
Open Stage Frontier 02(전선 02단계) as `stage_frontier_02__joint_objective_onnx_density_quality_scout`.
This is a stage-open review(단계 개방 검토), not an experiment result(실험 결과 아님), not completion(완성 아님), not baseline(기준선 아님), not promotion(승격 아님), not runtime authority(런타임 권위 아님), not live readiness(실거래 준비 아님), and not Goal Achieve(목표 달성 아님).

Current truth(현재 진실):
- Stage Frontier 01(전선 01단계) is closed as archive interface(보관소 접점) only.
- Stage12~364(12~364단계) are reference only(참조 전용): reference, not inheritance(참조이지 상속 아님).
- Stage364(364단계) closed with strict_joint_pass_count(엄격 동시 통과 수)=0 and preserved clue(보존 단서) `hold4_margin_0.01` net/PF/density(순수익/수익 팩터/밀도)=462.0071630903 / 1.2257899553 / 2.1178343949.
- No winner/baseline/promotion/runtime authority/live readiness/Goal Achieve(승자/기준선/승격/런타임 권위/실거래 준비/목표 달성) is inherited.

Proposed Frontier 02 thesis(제안 전선 02 가설):
A directly trained ONNX(온엑스) surface(표면) using a four-axis joint objective(네 축 동시 목적) can approach the final target better than the prior one-axis repair loop(한 축 수리 반복) by evaluating density/PF/DD/curve smoothness(밀도/수익 팩터/손실폭/곡선 매끄러움) together from the first proxy design(첫 프록시 설계).

Final target(최종 목표) remains hard only at final completion review(최종 완성 검토):
1. 5~10 trades/day(일 5~10회 거래)
2. PF(profit factor, 수익 팩터) 2~3+
3. DD(drawdown, 손실폭) under 10% in any zoomed segment(확대 구간)
4. smooth upward balance/equity curve(매끄러운 잔고/자산 우상향)

Stage-open success criteria(단계 개방 성공 기준):
- frontier_thesis(전선 가설), novelty_delta(신규성 차이), prior_stage_scan(이전 단계 점검), do_not_repeat(반복 금지), exit_rule(종료 규칙), claim_boundary(주장 경계) are explicit.
- First packet is proxy design/scout only(프록시 설계/탐색 전용); no WFO/MT5(워크포워드 최적화/메타트레이더5) until scout clue(탐색 단서) and pre-expensive-review(비싼 검증 전 검토) justify it.
- Tier A separate/Tier B separate/Tier A+B combined(티어 A 분리/티어 B 분리/티어 A+B 합산) records are required, or explicit missing_required/blocked/out_of_scope_by_claim(필수 누락/차단/주장 범위 밖).

DNR constraints(반복 금지 제약):
- Do not inherit winner/baseline/promotion(승자/기준선/승격 상속 금지).
- Do not advance a one-axis improvement(한 축 개선만으로 전진 금지).
- Do not call proxy proof runtime proof(프록시 증명을 런타임 증명으로 부르기 금지).
- Do not repeat sparse PF selector(희소 PF 선택기 반복 금지).
- Do not ignore Tier B/combined rows(Tier B/합산 행 무시 금지).
- Do not split the same live hypothesis by impatience(진행 중 가설을 조급하게 쪼개기 금지).

Codex proposed first work packet(제안 첫 작업 묶음):
1. Create stage structure(단계 구조) under `stages/stage_frontier_02__joint_objective_onnx_density_quality_scout/`.
2. Write stage brief(단계 요약), prior-stage reference scan(이전 단계 참조 점검), experiment design(실험 설계), proxy score plan(프록시 점수 계획), selection status(선택 상태), stage ledger header(단계 장부 헤더), and review index(검토 색인).
3. Record this Grok stage-open review(그록 단계 개방 검토) and local verification(로컬 검증).
4. Update current truth docs(현재 진실 문서) to active Frontier 02(활성 전선 02) with no authority claims(권위 주장 없음).

Review questions(검토 질문):
- Is the proposed stage id(제안 단계 ID) and thesis(가설) sufficiently independent(충분히 독립적) from Stage12~364(12~364단계), or does it still leak inheritance(상속 누수)?
- Is the four-axis joint objective(네 축 동시 목적) a real novelty_delta(신규성 차이), or just the old repair loop(기존 수리 반복) renamed?
- What should be changed before Codex opens the stage(단계 개방) and writes the first proxy/scout design(첫 프록시/탐색 설계)?
- What local verification(로컬 검증) must Codex perform before saying stage-open review captured(단계 개방 검토 기록됨)?

Please answer in exactly three sections(정확히 세 구역): accepted(수용), rejected(거절), needs_local_verification(로컬 검증 필요). Keep it concise(짧게), and do not claim completion/baseline/promotion/runtime authority/live readiness/Goal Achieve(완성/기준선/승격/런타임 권위/실거래 준비/목표 달성 주장 금지).
