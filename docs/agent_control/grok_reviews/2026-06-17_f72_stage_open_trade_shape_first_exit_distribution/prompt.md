# F72 Stage Open Review Prompt(F72 단계 개방 검토 프롬프트)

You are Grok(Grok, 그록), external second opinion(외부 2차 의견) for Project Obsidian Prime v2.

Answer only from this prompt(프롬프트) as snapshot-only direct answer(스냅샷 전용 직접 답변). Do not inspect files(파일 확인 금지), run tools(도구 실행 금지), browse(브라우징 금지), or perform local verification(로컬 검증 금지). Codex(코덱스) will do local verification(로컬 검증).

## Current Truth(현재 진실)

- Workspace state(작업공간 상태): F71 is closed(마감) as `closed_preserved_clue_negative_memory_no_authority`.
- Next run(다음 실행): `frontier72A_stage_open_new_upstream_axis_after_f71_economics_negative_memory_v1`.
- Five-stage retrospective(5단계 중간 검토): `not_due_after_f71_closeout`, one closeout after F66-F70 retrospective(F66-F70 중간 검토 뒤 1개 마감).
- Git/publish boundary(깃/게시 경계): local branch(로컬 브랜치) is ahead of origin by 4 commits(원격보다 4커밋 앞섬); F71 push(원격 반영)는 broad code-surface audit(코드 표면 감사) failure(실패) 때문에 blocked(차단)로 기록됨.
- Forbidden claims(금지 주장): no completion/baseline/promotion/runtime authority/live readiness/Goal Achieve(완성/기준선/승격/런타임 권위/실거래 준비/목표 달성 없음).

## F71 Evidence(근거)

- F71 hypothesis(가설): economics-native label/target and selection(경제성 네이티브 라벨/목표와 선택)이 density/PF/DD(밀도/수익 팩터/손실폭)를 함께 보존하는 seed surface(씨앗 표면)를 만들 수 있는가.
- F71 proxy(프록시): F71B candidates(후보) 1620, scout clue(탐색 단서) 9, meaningful candidate(의미 후보) 0. F71C repair candidates(수리 후보) 1440, scout clue(탐색 단서) 3, meaningful candidate(의미 후보) 0.
- F71 runtime probe(런타임 탐침): F71D had signal count gap(신호 수 간극), feature diff(피처 차이) 0.
- F71 repair(수리): F71E edge_margin q40(EA 호환 엣지 마진 q40) repaired signal/feature parity(신호/피처 동등성) exactly. Runtime OOS(표본외 런타임) stayed weak: net(순수익) 36.35, PF(수익 팩터) 1.09, DD(손실폭) 5.92%, trades/day(일 거래 수) 1.3231.
- F71 preserved clue(보존 단서): EA-compatible selection semantics(EA 호환 선택 의미)이 parity(동등성)를 복구할 수 있음.
- F71 negative memory(부정 기억): same economics-native label/model/selection surface(같은 경제성 네이티브 라벨/모델/선택 표면)를 q threshold or tape-only sweep(q 임계값 또는 테이프 단독 훑기)으로 반복하지 말 것.

## Proposed F72 Direction(Codex 방향 제안)

Open `stage_frontier_72__trade_shape_first_exit_distribution_and_risk_guard_labeling`.

Hypothesis(가설): trade-shape-first exit distribution and risk-guard labeling(거래 형태 우선 청산 분포 및 위험 보호 라벨링)이 F71의 economics-native threshold surface(경제성 네이티브 임계값 표면)보다 더 넓은 density/PF/DD seed surface(밀도/수익 팩터/손실폭 씨앗 표면)를 만들 수 있는지 시험한다.

Core axis change(핵심 축 변화):

- trade shape(거래 형태)가 lead axis(주도 축)다: entry/exit/hold-time/adverse excursion(진입/청산/보유시간/불리한 변동)을 먼저 정의한다.
- label/target(라벨/목표)은 probability-only(확률 단독)나 F71 economics-native class(경제성 네이티브 클래스)가 아니라 exit path quality(청산 경로 품질), MAE/MFE balance(불리/유리 이동 균형), time-under-water(회복 전 체류 시간), stop/target feasibility(손절/익절 가능성)를 맞히게 한다.
- feature set(피처 묶음)은 ablation/recombination(빼기/재조합)을 stage rule(단계 규칙)에 포함한다: core price/session/volatility/path features(핵심 가격/세션/변동성/경로 피처)를 묶음별로 제거/교체/재조합한다.
- model family(모델 계열)는 initial broad sweep(초기 넓은 훑기)로 linear/logistic(선형/로지스틱), EBM-like additive tree(EBM 유사 가산 트리), small NN(작은 신경망), tree reference(트리 참조)를 비교한다.
- risk logic(위험 로직)은 label(라벨)과 selection(선택)에 내장한다: SL/TP/time stop/DD guard(손절/익절/시간 청산/손실폭 보호)를 프록시 단계에서 명시한다.
- regime/session split(장세/세션 분할)은 first-class view(일급 보기)로 남긴다: cash open/mid/late(정규장 초/중/후반), trend/chop/vol expansion(추세/횡보/변동성 확대).

## F72 Stage-Open Guardrails(F72 단계 개방 보호 규칙)

- This is stage open design only(단계 개방 설계 전용), no runtime result yet(아직 런타임 결과 없음).
- Mandatory lifecycle(필수 생명주기)는 그대로 유지: proxy(프록시) -> mandatory MT5 Runtime Probe(필수 MT5 런타임 탐침) -> proxy/runtime gap analysis(프록시/런타임 간극 분석) -> repair(수리) -> closeout(마감).
- MT5 Runtime Probe(MT5 런타임 탐침)는 F72 안에서 반드시 실행해야 한다. Stage open(단계 개방)만으로 완료나 권위는 만들지 않는다.
- Tier records(티어 기록): Tier A separate(Tier A 분리), Tier B separate(Tier B 분리), Tier A+B combined(Tier A+B 합산)를 계획하고, 만들 수 없으면 missing_required/out_of_scope_by_claim(필수 누락/주장 범위 밖)로 기록한다.
- Do-not-repeat(반복 금지): F71 q threshold/tape-only sweep(F71 q 임계값/테이프 단독 훑기), F70 same label-regime density surface(F70 같은 라벨-장세 밀도 표면), F69 trade-shape-only post-hoc throttling(F69 거래 형태 단독 사후 제한)은 금지한다. F72 may use trade shape(거래 형태)는 lead axis(주도 축)이지만 post-hoc rescue(사후 구제)가 아니라 label/exit/risk construction(라벨/청산/위험 구성)이어야 한다.

## Review Question(검토 질문)

Critique this F72 opening direction(이 F72 개방 방향을 비판) with three outputs only:

1. accepted(수용): what is valid about the proposed F72 axis(제안된 F72 축에서 유효한 점).
2. rejected(거절): what should not be done because it repeats F69/F70/F71 or creates forbidden claims(반복 또는 금지 주장 때문에 하지 말아야 할 점).
3. needs_local_verification(로컬 검증 필요): what Codex must verify locally before materializing F72 stage-open artifacts(F72 단계 개방 산출물 물질화 전 로컬 확인 사항).

Do not propose completion(완성), baseline(기준선), promotion(승격), runtime authority(런타임 권위), live readiness(실거래 준비), or Goal Achieve(목표 달성).
