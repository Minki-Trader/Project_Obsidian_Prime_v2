# F71 Stage Open Review(F71 단계 개방 검토)

You are Grok(Grok, 그록), an external second opinion(외부 2차 의견).
Answer only from this bounded evidence(제한 근거). Do not inspect files, do not use tools, and do not create completion(완성), baseline(기준선), promotion(승격), runtime authority(런타임 권위), live readiness(실거래 준비), or Goal Achieve(목표 달성).

## Current Truth(현재 진실)

- F70 closed(마감) as `preserved_clue_negative_memory_no_authority(보존 단서 + 부정 기억, 권위 없음)`.
- F66-F70 five-stage retrospective(5단계 중간 검토) is completed(완료) and current_due_status(현재 도래 상태) is `not_due_after_retrospective_completed(중간 검토 완료 후 아직 아님)`.
- Retrospective direction_delta(방향 변화): pivot to economics-native model/label/selection(경제성 네이티브 모델/라벨/선택).
- Retrospective repair_priority_delta(수리 우선순위 변화): proxy economics joint gate first(프록시 경제성 공동 게이트 우선), runtime probe second(런타임 탐침은 이후).

## Proposed F71 Canonical Stage(제안 F71 정식 단계)

`stage_frontier_71__economics_native_label_selection_for_joint_density_pf_dd`

## Proposed Hypothesis(제안 가설)

An economics-native label/selection objective(경제성 네이티브 라벨/선택 목표), trained to select entries that survive a joint density/PF/DD gate(밀도/수익 팩터/손실폭 공동 게이트), may produce more meaningful candidates(의미 후보) than F68-F70 bridge/risk/tape repairs(연결/위험/테이프 수리).

Effect(효과): F71 changes what is being selected(무엇을 선택하는지) rather than only changing how selected entries are throttled(선택 진입을 어떻게 제한하는지).

## F71 Must-Change Axes(F71 필수 변경 축)

- label/target(라벨/목표): from direction/first-hit/regime value(방향/최초 도달/장세 가치) to realized lifecycle economics(실현 생명주기 경제성), including cost/slippage/swap-aware PnL(비용/슬리피지/스왑 인식 손익), MFE/MAE balance(최대 유리/불리 움직임 균형), and adverse path stress(불리 경로 압박).
- selection objective(선택 목표): select candidate entries only if they help joint density/PF/DD(밀도/수익 팩터/손실폭 공동 조건), not only probability rank(확률 순위).
- model family(모델 계열): allow linear(선형), EBM-like additive(설명 가능 가산형), LGBM/ExtraTrees reference(참조), and small NN(작은 신경망) only if they express the economics target(경제성 목표).
- feature set(피처 묶음): test feature removal/recombination(피처 제거/재조합) and not only F70 macro/regime surface(F70 거시/장세 표면).
- trade shape(거래 형태): include entry/exit/hold-time constraints(진입/청산/보유시간 제약) as part of label evaluation(라벨 평가), not post-hoc(사후) throttling.
- risk logic(위험 로직): evaluate SL/TP and DD guard(손절/익절 및 손실폭 보호)를 label economics(라벨 경제성)에 내장한 variants(변형).
- regime/session split(장세/세션 분할): treat regimes as evaluation slices(평가 조각), not a single inherited filter(상속 필터).

## Guardrails(보호 장치)

- No F70 same-surface tape/threshold/cooldown/quota repeat(F70 같은 표면 테이프/임계값/쿨다운/할당 반복 금지).
- No sparse PF standalone read(희소 수익 팩터 단독 판독 금지) without density-lift fracture test(밀도 상승 균열 시험).
- No runtime probe(런타임 탐침) as primary discovery(주 탐색) before proxy has meaningful candidate evidence(의미 후보 근거), but every frontier stage(전선 단계)는 still requires MT5 Runtime Probe(MT5 런타임 탐침).
- Runtime probe(런타임 탐침) purpose: confirm economics transfer(경제성 이전 확인), not re-prove bridge parity(연결 동등성 재증명).

## Initial F71 Lifecycle Plan(초기 F71 생명주기 계획)

1. F71A stage open(단계 개방): define economics-native label/selection design(경제성 네이티브 라벨/선택 설계).
2. F71B proxy scout(프록시 탐색): broad label/feature/model/trade-shape surface(넓은 라벨/피처/모델/거래 형태 표면).
3. F71C repair/recombine(수리/재조합): only if proxy creates meaningful clue(의미 단서).
4. F71D mandatory MT5 Runtime Probe(필수 MT5 런타임 탐침): materialize at least one diagnostic axis(진단 축) if proxy has signal(신호), otherwise record blocked/invalid setup(차단/무효 설정) with repair action(수리 행동).
5. F71E gap analysis/repair(간극 분석/수리), then closeout(마감).

## Questions(질문)

1. Is F71 sufficiently new(충분히 새로움) relative to F70, or is it still same-surface repair(같은 표면 수리)?
2. Which axis should lead(주도 축): label/target(라벨/목표), selection objective(선택 목표), feature set(피처 묶음), model family(모델 계열), trade shape(거래 형태), risk logic(위험 로직), or regime/session split(장세/세션 분할)?
3. What should Codex reject(거절) if it appears during F71?
4. What local verification(로컬 검증) is needed before acting?
5. What claim boundary(주장 경계) should remain?

Return concise classification(간단 분류):

- accepted(수용)
- rejected(거절)
- needs_local_verification(로컬 검증 필요)
