Project Obsidian Prime v2 bounded Grok review(제한 그록 검토)

Review size(검토 크기): small review(소규모 검토). One narrow question(좁은 질문): Is the proposed Frontier20(전선20) stage-open(단계 개방) valid as a new hypothesis lifecycle(가설 생명주기), or is it just a repeat of recent negative memories(부정 기억)?

Current truth(현재 진실): Frontier19(전선19) closed as negative_memory(부정 기억). It proved capped boosted-tree backbone-only(상한 부스팅 트리 백본 단독) can export valid ONNX(유효 온엑스) 4/4, but no strict/seed/preserved/handoff rows(엄격/씨앗/보존/인계 행 0/0/0/0). No completion(완성), baseline(기준선), promotion(승격), runtime authority(런타임 권위), live readiness(실거래 준비), or Goal Achieve(목표 달성).

Do-not-repeat(반복 금지): F05 feature micro-expansion(피처 미세 확장) failed; F15 probability threshold grid(확률 임계값 격자) failed; F16 edge-quality risk-veto(엣지 품질 위험 배제) failed; F18 lifecycle parameter repair(생명주기 파라미터 수리) failed; F19 boosted backbone-only(부스팅 백본 단독) failed. Avoid threshold micro-search(임계값 미세 탐색), validation-guided filtering(검증 유도 필터링), quota/flat subset repair(할당량/평면 부분 수리), and backbone-only rerun(백본 단독 재실행).

Codex proposed direction(코덱스 제안 방향): open `stage_frontier_20__train_only_feature_state_rule_atlas_onnx_scout`(학습 전용 피처 상태 규칙 지도 온엑스 탐색). Hypothesis(가설): sparse closed-bar feature-state conjunctions(희소 확정봉 피처 상태 결합), selected only on train split(학습 분할에서만 선택), may expose direct alpha surfaces(직접 알파 표면) that model probability/backbone approaches washed out. Later, if a surface survives validation/OOS(검증/표본외), Codex may distill or encode it into ONNX(온엑스) representation.

Local pre-open probe(로컬 사전 탐침), in-memory only(메모리 내 전용): with fixed 58 contract features(고정 58 계약 피처), top train-selected pair examples around `vix_zscore_20 <= q30` plus momentum(모멘텀) showed validation/OOS PF(검증/표본외 수익 팩터) roughly 1.22~1.43 / 1.18~1.29, density(빈도) about 6~10/day, but DD(손실폭) still high. This is not a completion candidate(완성 후보); at most a seed surface(씨앗 표면) direction.

Proposed locks(제안 잠금): use existing 58 features only(기존 58개 피처만), no new feature engineering(새 피처 설계 없음), max conjunction depth 2(결합 깊이 최대 2), fixed train quantiles q10/q20/q30/q70/q80/q90(고정 학습 분위수), side chosen on train only(방향은 학습 전용), validation/OOS read-only(검증/표본외 읽기 전용), no probability thresholds(확률 임계값 없음), no boosted backbone(부스팅 백본 없음), no lifecycle/quota/firewall repair(생명주기/할당량/방화벽 수리 없음), no runtime authority claim(런타임 권위 주장 없음). Runtime probe(MT5 런타임 탐침) only if a handoff candidate(인계 후보) exists; otherwise record exact blocker(정확한 차단 사유 기록).

Success criteria for early exploration(초기 탐색 성공 기준): scout clue(탐색 단서) if validation and OOS(검증과 표본외) move toward 5~10 trades/day(일 5~10회), PF 1.5+ early target(초기 목표 수익 팩터 1.5 이상), DD materially lower(손실폭 의미 있게 낮음), and smoother curve(더 매끄러운 곡선). Final hard gates(최종 강제 게이트)는 not applied yet(아직 적용하지 않음).

Question(질문): Accept/reject/adjust(수용/거절/조정) this as Frontier20 stage-open(전선20 단계 개방). Name any mandatory locks(필수 잠금) needed to prevent repeating F05/F15/F19, and any local verification(로컬 검증) Codex must perform before materializing F20A.
