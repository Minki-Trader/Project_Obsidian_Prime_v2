# Codex Receipt(Codex 영수증): ONNX Frontier Goal Prompt(ONNX 전선 목표 프롬프트)

## Trigger Reason(트리거 이유)

User asked(사용자 요청) for Grok advice(Grok 조언) on an intuitive goal prompt(직관적 목표 프롬프트) that is not overly complex(과하게 복잡하지 않음) but can keep Codex(코덱스) progressing until a genuinely excellent ONNX(진짜 뛰어난 온엑스)가 produced(생성)된다.

## Review Size(검토 크기)

medium review(중간 검토)

## Direction Before Grok(Grok 전 방향)

Codex draft direction(Codex 초안 방향): keep the prompt to five parts(다섯 부분): final target(최종 목표), operating loop(운영 루프), evidence discipline(근거 규율), Grok collaboration(Grok 협업), and claim boundary(주장 경계).

## Bounded Evidence(제한 근거)

- Active stage(활성 단계): `stage_frontier_01__archive_synthesis_and_new_axis_lock`
- Next run(다음 실행): `frontier01B_build_stage12_364_campaign_map_v1`
- Final ONNX target(최종 ONNX 목표): 5-10 trades/day(하루 5-10회 거래), PF 2-3x(PF 2-3배), DD <10%(손실폭 10% 미만), inspected intervals(검토 구간), smooth rising equity curve(매끄러운 상승 자산 곡선).
- Accepted operating model(수용한 운영 모델): one frontier stage(하나의 전선 단계) equals one hypothesis lifecycle(하나의 가설 생명주기).

## Prompt Identity(프롬프트 정체성)

- Path(경로): `docs/agent_control/grok_reviews/2026-06-13_goal_prompt_for_onnx_frontier/medium_review/prompt.md`
- Hash(해시): `25898748c5b5588e1a16bbacdc65183e446573c0a99f58af7b0447ea10f93b91`

## Grok Output Identity(Grok 출력 정체성)

- Path(경로): `docs/agent_control/grok_reviews/2026-06-13_goal_prompt_for_onnx_frontier/medium_review/clean_output.md`
- Metadata(메타데이터): `docs/agent_control/grok_reviews/2026-06-13_goal_prompt_for_onnx_frontier/medium_review/metadata.json`
- Transport result(전송 결과): success(성공), returncode(반환 코드) `0`, timed_out(시간 초과) `false`
- Hash(해시): `unavailable_after_encoding_repair`

## Advice Classification(조언 분류)

accepted(수용):

- Goal prompt(목표 프롬프트)는 north star(북극성), keep pushing(계속 밀기), no fake completion(가짜 완료 금지) 구조가 좋다.
- The final four axes(최종 4축)는 final completion review(최종 완성 검토)에서만 hard gate(강한 게이트)로 둔다.
- One frontier stage(하나의 전선 단계)는 one hypothesis lifecycle(하나의 가설 생명주기)로 둔다.
- Grok(Grok)은 stage open(단계 개방), expensive WFO/MT5 before-run(비싼 WFO/MT5 전), closeout(마감)에서 second opinion(2차 의견)으로 둔다.

rejected(거절):

- Full 11-layer evidence list(전체 11층 근거 목록)를 user goal prompt(사용자 목표 프롬프트)에 넣는 방식.
- PF 2-3x(PF 2-3배)를 scout cutoff(탐색 절단 기준)로 두는 방식.
- Middle work(중간 작업)에서 completion(완성), baseline(기준선), runtime authority(런타임 권위), live readiness(실거래 준비)를 말하는 방식.

needs_local_verification(로컬 검증 필요):

- Exact wording(정확한 문구)은 user tone(사용자 말투)에 맞춰 최종 조정한다.
- 4-axis simultaneous feasibility(4축 동시 가능성)는 실제 data(데이터)와 MT5 evidence(MT5 근거)로만 확인할 수 있다.

## Claim Boundary(주장 경계)

No operating promotion(운영 승격), runtime authority(런타임 권위), live readiness(실거래 준비), selected baseline(선택 기준선), or Goal Achieve(목표 달성) is claimed.

## Final Codex Direction(최종 Codex 방향)

Use a compact goal prompt(간결한 목표 프롬프트) centered on north star(북극성), reference-only archive(참조 전용 보관), and claim boundary(주장 경계). Effect(효과): Codex can keep pushing toward the excellent ONNX(뛰어난 ONNX) without turning the user prompt into a policy manual(정책 매뉴얼).
