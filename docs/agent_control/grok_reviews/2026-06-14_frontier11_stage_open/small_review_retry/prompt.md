Frontier11 stage-open review retry(전선11 단계 개방 검토 재시도)입니다.

Please answer in this response only(이 응답 안에서만 답하세요). Do not say you will write a file(파일을 쓰겠다고 말하지 마세요).

Current truth(현재 진실):
- Frontier10(전선10)은 `closed_preserved_clue_negative_memory_no_authority`.
- F10 failure memory(F10 실패 기억): validation DD(검증 손실폭) 56~60%, strict rows(엄격 행) 0, side-weight repair(방향 가중 수리)는 OOS DD(표본밖 손실폭)를 악화.
- No baseline/promotion/runtime/live/Goal claim(기준선/승격/런타임/실거래/목표 주장 없음).

Codex proposed next frontier(코덱스 제안 다음 전선):
- `stage_frontier_11__subperiod_stability_first_onnx_scout`
- New hypothesis(새 가설): choose fixed 3-class ONNX candidates(고정 3분류 온엑스 후보) by subperiod stability(하위기간 안정성), worst-slice DD(최악 구간 손실폭), time-under-water proxy(회복 전 체류 시간 프록시), and equity smoothness proxy(자산곡선 매끄러움 프록시), not just aggregate validation/OOS(검증/표본밖 합계).
- Novelty(신규성): changes validation philosophy/candidate selection surface(검증 철학/후보 선택 표면), not label/objective/weight/bridge(라벨/목적/가중/브리지).
- Do not repeat(반복 금지): same side-weight ladder(같은 방향 가중 사다리), density bridge(밀도 브리지), threshold micro-search(임계값 미세 탐색).

Required output(필수 출력):
1. Classification(분류): accepted/rejected/needs_local_verification(수용/거절/로컬 검증 필요).
2. One-sentence reason(한 문장 이유).
3. Required records(필수 기록): what Codex must record to avoid overclaiming(과장 주장 방지 기록).
4. Forbidden claims check(금지 주장 확인).
