Frontier11 stage-open review(전선11 단계 개방 검토) small review(소규모 검토)입니다.

Codex current truth(코덱스 현재 진실):
- Frontier10(전선10)은 `closed_preserved_clue_negative_memory_no_authority`로 닫혔습니다.
- Frontier10 preserved clue(전선10 보존 단서): utility-margin target(효용 마진 목표), modest side-class weighting(완만한 방향 클래스 가중), split-consistent leakage guard(분할 일관 누수 보호).
- Frontier10 negative memory(전선10 부정 기억): validation DD(검증 손실폭) 56~60%, strict rows(엄격 행) 0, best preserved repair(최상 보존 수리)도 OOS DD(표본밖 손실폭) 악화.
- Forbidden claims(금지 주장): completion/baseline/promotion/runtime authority/live readiness/Goal Achieve(완성/기준선/승격/런타임 권위/실거래 준비/목표 달성) 없음.

Codex proposed Frontier11(코덱스 제안 전선11):
- Stage id(단계 ID): `stage_frontier_11__subperiod_stability_first_onnx_scout`
- Hypothesis(가설): fixed 3-class ONNX(고정 3분류 온엑스)를 고를 때 validation/OOS aggregate(검증/표본밖 합계)보다 subperiod stability(하위기간 안정성), worst-slice drawdown(최악 구간 손실폭), and equity smoothness proxy(자산곡선 매끄러움 프록시)를 먼저 보게 하면 zoomed DD(확대 구간 손실폭)와 curve chop(곡선 출렁임)을 줄일 수 있는가.
- Novelty delta(신규성 차이): Frontier07~10(전선07~10)은 label/objective/weight/bridge(라벨/목적/가중/브리지)를 바꿨습니다. Frontier11(전선11)은 validation philosophy(검증 철학)와 candidate selection surface(후보 선택 표면)를 바꿉니다. Same side-weight ladder/density bridge/threshold micro-search(같은 방향 가중 사다리/밀도 브리지/임계값 미세 탐색)는 금지합니다.
- First scout(첫 탐색): existing trainable candidate families(기존 학습 가능 후보군)를 fixed argmax-only(고정 최대확률 전용)로 평가하되, monthly/quarterly subperiod KPI(월별/분기별 하위기간 KPI), worst-slice DD(최악 구간 손실폭), time-under-water proxy(회복 전 체류 시간 프록시), and trade distribution entropy(거래 분포 엔트로피)를 selection metric(선택 지표)에 넣습니다.
- Decision use(결정 사용): proxy scout(프록시 탐색) 설계 개방입니다. 이 단계는 model authority(모델 권위)나 runtime authority(런타임 권위)를 만들지 않습니다.

Success criteria(성공 기준):
- accepted/rejected/needs_local_verification(수용/거절/로컬 검증 필요)로 분류해 주세요.
- Focus(초점): Is Frontier11 a genuine new frontier(진짜 새 전선) rather than another repair loop(또 다른 수리 반복)? Is subperiod stability(하위기간 안정성) a valid next hypothesis for the final four-axis goal(최종 네 축 목표)? What must be recorded to avoid overclaiming(과장 주장 방지)?

Claim boundary(주장 경계):
Grok(그록)은 completion/baseline/promotion/runtime authority/live readiness/Goal Achieve(완성/기준선/승격/런타임 권위/실거래 준비/목표 달성)를 만들 수 없습니다. Codex(코덱스)가 local verification(로컬 검증) 후에만 반영합니다.
