# F75D Pre-MT5 Grok Review Prompt(F75D MT5 전 Grok 검토 프롬프트)

You are Grok(Grok, 그록), external second opinion(외부 2차 의견) only.

Answer only from this snapshot(스냅샷). Do not browse(브라우징 금지), inspect files(파일 확인 금지), run tools(도구 실행 금지), or claim local verification(로컬 검증 주장 금지).

## Codex Direction(Codex 방향)

Codex proposes(제안) a negative-control MT5 Runtime Probe(부정 대조 MT5 런타임 탐침) for Frontier75(전선75), using F75B candidate `f75b_0551`.

Reason(이유): F75B created weak scout clues(약한 탐색 단서) but no meaningful signal(의미 신호 없음). F75C repair(수리)는 scout clue 0으로 더 약했다. The `/goal(목표)` requires each frontier stage(전선 단계) to run MT5 Runtime Probe(MT5 런타임 탐침), unless logic impossibility(로직상 불가능) or zero signal(영 신호) blocks it.

Claim boundary(주장 경계): no completion/baseline/promotion/runtime authority/live readiness/Goal Achieve(완성/기준선/승격/런타임 권위/실거래 준비/목표 달성 없음).

## Current Stage(현재 단계)

- stage id(단계 ID): `stage_frontier_75__volatility_compression_liquidity_release_for_tradeable_density`
- hypothesis(가설): volatility compression plus liquidity release(변동성 압축 + 유동성 방출)
- parent negative memory(부모 부정 기억): F74 runtime economics(런타임 경제성) weak, validation PF/DD/tpd(검증 수익 팩터/손실폭/일거래) `1.16/11.40%/1.6544`, OOS(표본외) `1.13/9.66%/1.60`.

## F75A Stage Open(F75A 단계 개방)

- Grok stage-open advice(Grok 단계 개방 조언): accepted(수용)
- accepted repair priority(수용된 수리 우선순위): put SL/TP and MAE/MFE(손절/익절 및 최대 불리/유리 이동) into labels/proxy simulation(라벨/프록시 시뮬레이션)
- context anchor(맥락 고정점): active, goal resume(목표 재개) reads state/context/anchor first

## F75B Proxy Scout(F75B 프록시 탐색)

Design(설계): broad compression/session/feature/model/risk sweep(압축/세션/피처/모델/위험 넓은 탐색)

KPI(핵심 성과 지표):

- candidate rows(후보 수): `594`
- scout clue count(탐색 단서 수): `11`
- meaningful signal count(의미 신호 수): `0`
- final-like reference only count(최종형 참고 전용 수): `0`

Best candidate(최선 후보), `f75b_0551`:

- direction(방향): short(숏)
- horizon(보유 수평선): `18` M5 bars
- context gate(맥락 조건): `hv_q35_compression`
- session gate(세션 조건): `cash_all`
- feature bundle(피처 묶음): `all58`
- model family(모델 계열): `extra_trees_d7_l80`
- target trades/day(목표 일거래): `5.0`
- validation(검증): net/PF/DD/tpd/win/expectancy/recovery/trades = `2292.5432 / 1.8815 / 2.6469% / 0.9016 / 51.52% / 13.8942 / 8.6612 / 165`
- OOS(표본외): net/PF/DD/tpd/win/expectancy/recovery/trades = `514.0273 / 1.1963 / 5.6023% / 1.0000 / 44.27% / 3.9239 / 0.9175 / 131`
- classification(분류): scout clue(탐색 단서), not meaningful signal(의미 신호 아님)

Interpretation(해석): cross-split positive but low density and weak OOS PF(교차 분할 양수지만 낮은 밀도와 약한 표본외 수익 팩터).

## F75C Repair Proxy(F75C 수리 프록시)

Design(설계): prior compression + current release trigger short setup(직전 압축 + 현재 방출 트리거 숏 설정)

KPI(핵심 성과 지표):

- candidate rows(후보 수): `324`
- scout clue count(탐색 단서 수): `0`
- meaningful signal count(의미 신호 수): `0`

Best candidate(최선 후보), `f75c_0286`:

- direction(방향): short(숏)
- horizon(보유 수평선): `12` M5 bars
- context gate(맥락 조건): `repair_rule_score_q70`
- feature bundle(피처 묶음): `repair_macro`
- model family(모델 계열): `logistic_l2_balanced`
- validation(검증): net/PF/DD/tpd/win/expectancy/recovery/trades = `40.9363 / 1.0121 / 5.7546% / 1.4536 / 36.09% / 0.1539 / 0.0711 / 266`
- OOS(표본외): net/PF/DD/tpd/win/expectancy/recovery/trades = `848.9639 / 1.3312 / 4.2434% / 1.5115 / 44.44% / 4.2877 / 2.0007 / 198`
- classification(분류): no scout clue(탐색 단서 없음) because validation PF is flat(검증 수익 팩터 평평함)

Interpretation(해석): OOS stronger than F75B but validation not confirmatory(표본외는 강하지만 검증 확인 없음).

## Codex Proposed F75D Action(Codex 제안 F75D 행동)

Run pre-MT5 packet( MT5 전 묶음) accepting a negative-control runtime probe(부정 대조 런타임 탐침), target `f75b_0551`, because it is the only cross-split scout clue(교차 분할 탐색 단서) and preserves the original F75 compression hypothesis(압축 가설).

Expected runtime observation(예상 런타임 관찰): likely weak PF/trade density(약한 수익 팩터/거래 밀도), but useful proxy/runtime gap evidence(프록시/런타임 간극 근거).

## Questions(질문)

1. Should Codex accept, reject, or modify this negative-control MT5 Runtime Probe(부정 대조 MT5 런타임 탐침) plan?
2. Should the MT5 probe target `f75b_0551`, `f75c_0286`, or both? Explain briefly.
3. What specific proxy/runtime gap risks(프록시/런타임 간극 위험) should Codex record before execution?

Allowed claims(허용 주장): advice classification(조언 분류), risk notes(위험 메모), repair priority(수리 우선순위).

Forbidden claims(금지 주장): completion(완성), selected baseline(선택 기준선), promotion(승격), runtime authority(런타임 권위), live readiness(실거래 준비), Goal Achieve(목표 달성).
