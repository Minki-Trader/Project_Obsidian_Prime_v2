## 1. Classification

**needs_local_verification**

Codex’s direction—**preserved clue only**, **no broad WFO**, **one narrow MT5 runtime probe if closeout requires it**—is directionally sound under F17A, but the bounded evidence is not strong enough to treat runtime success as likely or to skip local readiness checks. ONNX parity passed is necessary, not sufficient (F16D precedent).

---

## 2. Recommendation

**Proceed with one narrow MT5 runtime probe now** — not broad WFO, not repair-only closeout — **unless local verification finds an exact blocker** (missing handoff, compile/manifest failure, tier routing mismatch, or no defined narrow probe contract).

Reasoning (critical):
- F17A default is probe-before-closeout unless **exactly blocked**; that guard should hold.
- F17B is **preserved_clue_candidate only**; the probe is for **runtime boundary evidence**, not promotion.
- OOS is only **weakly better** than F16B/F16D references (PF ~1.14, DD ~12.8%); that is a low bar and may not survive MT5.
- F16D shows **signal diff 0** can still yield **catastrophic OOS runtime DD**; skipping MT5 would leave closeout blind to the main failure mode this stage is meant to scout.

Do **not** interpret weak proxy superiority as justification for anything beyond a **single, narrowly scoped** probe.

---

## 3. Main failure risks

1. **Proxy–runtime gap (F16D pattern):** Validation PF 1.30 / DD 13.4% may not translate; OOS equity R² collapse (0.71 → 0.27) is a strong instability signal.
2. **Marginal OOS edge:** OOS PF 1.136 is thin; ~45.5% negative subperiods suggests inconsistent behavior, not a durable clue.
3. **Over-scoping the probe:** Treating “narrow” as WFO, multi-candidate sweep, or tier-mixed routing would violate F17A and burn cost without claim upgrade rights.
4. **False parity comfort:** ONNX parity passed does not guarantee tester parity on fills, spread, session gaps, or DD path.
5. **Claim creep:** Any positive runtime flicker could be misread as scout success, seed surface, or promotion path despite F17B boundaries.
6. **Closeout without negative memory:** If probe is skipped without documenting **exact blocker**, the stage loses honest runtime-negative evidence F16D already demonstrated matters.

---

## 4. Local verification Codex must do before acting

1. Confirm **exact narrow probe contract**: one candidate (`f17b_firewall_h10_ddq75_contq65__lr_plain__firewall_continuation`), fixed manifest/set, no sweep, no WFO expansion.
2. Verify **MT5 handoff readiness**: EA/module hash, `.set`/run manifest, ONNX bundle path, compile success, and artifact lineage tied to the F17B run identity.
3. Confirm **tier routing parity**: same Tier A / Tier B / combined interpretation as the preserved Python evidence; no synthetic sum mistaken for routed total.
4. Predefine **probe success/fail read**: runtime PF, DD, trade count, signal-diff vs Python; explicit stop if handoff breaks.
5. Check **F16D comparability**: same symbol, timeframe, broker contract, tester modeling assumptions; otherwise reference comparison is invalid.
6. If any item fails: record **exact blocker** and close out **without MT5** under F17A exception path—no retry loop disguised as “repair.”

---

## 5. Forbidden claim check

Codex must **not** claim from this evidence or from a narrow probe alone:

| Forbidden claim | Status |
|---|---|
| Stage/work packet **completion** | ❌ Not supported |
| **Baseline** / selected operating line | ❌ Not supported |
| **Promotion** / promotion candidate upgrade | ❌ Not supported |
| **Runtime authority** | ❌ Not supported |
| **Live readiness** | ❌ Not supported |
| **Goal Achieve** | ❌ Not supported |
| **Strict scout clue** / seed surface | ❌ Explicitly out of scope for F17B |

Allowed after probe (at most): **preserved_clue_candidate** with **runtime_probe** label—positive, negative, inconclusive, or **blocked**—and honest negative memory if runtime diverges like F16D. Probe outcome must **not** reopen broad WFO or raise claim boundary without a new gated work packet.
