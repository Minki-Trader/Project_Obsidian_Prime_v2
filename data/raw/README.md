# Raw Data

Keep source-native exports here before any contract-aligned transformation.

- `mt5_bars/` is for closed-bar `M5` source series by symbol.
- `real_ticks/` is for runtime or tester validation sources that need real-tick detail.
