# MT5 Bars

Keep manual or automated MetaTrader bar exports here.

The contract base frame is `US100` on `M5`, and external symbols should stay on their own raw `M5` series until they are merged by close timestamp.
