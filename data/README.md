# Data

This folder is the shared data root for the v2 restart.

- `raw/` holds broker-native source exports.
- `processed/` holds reusable shared outputs only.
- `snapshots/` holds frozen reusable dataset or runtime identity snapshots.

Heavy files stay local. Git tracks the directory skeleton and lightweight README files only.
