# Pipelines

Put reusable shared dataset, export, bundle, and validation pipelines here.

Prefer manifest-centered entry points over ad hoc stage-local scripts.

- `run_stage07_baseline_training_smoke.py` runs the Stage 07 baseline model training smoke and writes manifest-centered run evidence.
