# Models

Reusable model training and evaluation helpers belong here.

Pipelines may orchestrate runs, but reusable model logic should stay in this
owned module so model behavior does not become hidden inside stage scripts.
